---
phase: 02-conversion-led-homepage
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/content/assets.ts
  - src/content/homepage.ts
  - src/lib/quote-demo.ts
  - tests/homepage.test.mjs
  - tests/quote-demo.test.mjs
  - public/images/official/hino-300-series-hero.jpg
  - public/images/official/hino-200-series-card.jpg
  - public/images/official/hino-300-series-card.jpg
  - public/images/official/hino-500-series-card.jpg
  - public/images/official/hino-bus-puv-card.jpg
  - public/images/generated/hino-parts-service-workshop.png
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04]
must_haves:
  truths:
    - "The homepage selects only locally stored, authorized official Hino imagery with a maintained internal provenance record."
    - "A visitor's quote fields receive deterministic local validation without implying any lead was delivered."
    - "Truck range, value points, business uses, and local homepage copy are configurable rather than duplicated in components."
  artifacts:
    - path: src/content/assets.ts
      provides: Typed local official-asset manifest with source and Cebu-replacement metadata.
    - path: src/lib/quote-demo.ts
      provides: Pure quote-draft validation and non-delivery result contract.
    - path: tests/homepage.test.mjs
      provides: Executable official-asset provenance and local-path regression coverage before homepage section plans run.
    - path: public/images/official/hino-300-series-hero.jpg
      provides: Authorized local commercial hero visual.
  key_links:
    - from: src/content/assets.ts
      to: public/images/official/
      via: Local image src records with actual intrinsic dimensions.
      pattern: 'src: "/images/official/'
    - from: src/lib/quote-demo.ts
      to: tests/quote-demo.test.mjs
      via: Unit-tested validation exports.
      pattern: validateQuoteDraft
---

<objective>
Create the authoritative homepage-content, approved local-image, and presentational quote-validation contracts that every Phase 2 component consumes.

Purpose: preserve asset authorization/provenance and make the client quote experience truthful and testable before visual composition begins.
Output: local official files, typed manifest/homepage copy, and a tested local quote-demo state boundary.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@HINO_CEBU_WEBSITE_SPEC.md
@.planning/phases/02-conversion-led-homepage/02-CONTEXT.md
@.planning/phases/02-conversion-led-homepage/02-UI-SPEC.md
@.planning/phases/02-conversion-led-homepage/02-RESEARCH.md
@src/content/site.ts
@src/content/trucks.ts
@src/content/services.ts

<interfaces>
From src/content/trucks.ts:
```typescript
export interface TruckRange { slug: TruckSeriesSlug; name: string; category: string; description: string; href: string; availability: "requires-verification"; sourceUrl?: string; }
export const truckRanges: readonly TruckRange[];
export const businessUses: readonly string[];
```
From src/content/site.ts:
```typescript
export const siteConfig: { contact: { address: string; phone: { display: string; href: string }; directionsUrl: ConfiguredValue<string | null> }; hours: readonly { days: string; hours: string }[]; availabilityNotice: string; };
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Test and establish authorized local asset and homepage-content contracts</name>
  <files>src/content/assets.ts, src/content/homepage.ts, tests/homepage.test.mjs, public/images/official/hino-300-series-hero.jpg, public/images/official/hino-200-series-card.jpg, public/images/official/hino-300-series-card.jpg, public/images/official/hino-500-series-card.jpg, public/images/official/hino-bus-puv-card.jpg, public/images/generated/hino-parts-service-workshop.png</files>
  <behavior>
    - Every displayed Phase 2 visual is a local public file with provenance, replacement metadata, and measured intrinsic dimensions; the generated service placeholder is explicitly identified as non-official.
    - The hero record selects the visually reviewed official 300-Series commercial three-quarter box-truck image; four cards are consistent-light-background official images; the service record uses the approved generated workshop placeholder per D-04.
    - Homepage copy preserves no fabricated specs, availability, legal identity, or contact email and exposes value points/business-use data for rendering.
    - The manifest regression test rejects every non-local `src`, missing provenance field, false replacement flag, missing expected asset role, or source URL that differs from the exact approved official Hino Philippines URL for the selected local file.
  </behavior>
  <action>Create manifest tests that require the five official Hino assets to have local paths, exact Hino Philippines URLs, authorization metadata, and replacement flags. Require `partsServiceWorkshop` to use `/images/generated/hino-parts-service-workshop.png` with `sourceType: "generated-placeholder"`, `authorizedUse: false`, `replaceWithCebuPhoto: true`, and an internal-only replacement note. Create the typed manifest as the only homepage image source; metadata must never be rendered as public DOM attributes. Add homepage content records from existing configuration and retain the no-promotions constraint.</action>
  <verify><automated>node --test tests/homepage.test.mjs; Test-Path src/content/homepage.ts; (Get-ChildItem public/images/official -File | Measure-Object).Count -eq 6</automated></verify>
  <done>The Wave-1 manifest test passes against five exact local authorized official assets and one clearly marked generated service placeholder; homepage copy is config-driven and promotion-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test local quote validation before implementing its client form</name>
  <files>src/lib/quote-demo.ts, tests/quote-demo.test.mjs</files>
  <behavior>
    - Missing/invalid required name, mobile, email, company/business, vehicle, business use, units, and consent produces field-specific errors.
    - A valid draft produces a local confirmation result and never calls fetch, provider code, or claims delivery.
    - The vehicle value "Not sure — recommend a Hino for me" is valid and business-use choice does not alter vehicle interest.
  </behavior>
  <action>First create failing Node built-in tests for an exported quote draft type, validation function, errors/result type, required field order, consent, and truthful confirmation contract. Then implement the small pure TypeScript module to pass them. Keep no server endpoint or provider adapter in this phase (D-08); return only data the UI needs for local validation/loading/confirmation/safe-error presentation. Do not use any external package.</action>
  <verify><automated>node --test tests/quote-demo.test.mjs</automated></verify>
  <done>Required inputs have deterministic local errors, valid drafts return only a truthful local confirmation state, and vehicle interest remains independently selectable.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| official source download → local asset manifest | Authorized third-party visual files become public static assets. |
| visitor draft → local validation utility | Untrusted form values are processed only in-browser during Phase 2. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Tampering | official asset intake | mitigate | Record exact official image URLs, inspect selected files, use local copies only, and avoid unreviewed source claims. |
| T-02-02 | Information disclosure | asset manifest | mitigate | Keep provenance, authorization, and replacement metadata in typed content only; components later receive public rendering fields only. |
| T-02-03 | Tampering | quote draft | mitigate | Use a pure validation contract without HTML rendering, network calls, or provider behavior. |
| T-02-SC | Tampering | npm installs | accept | No package installation occurs in this plan. |
</threat_model>

<verification>
<automated>node --test tests/homepage.test.mjs</automated>
<automated>node --test tests/quote-demo.test.mjs</automated>
<automated>npm run lint</automated>
<automated>npm run build</automated>
</verification>

<success_criteria>
- Six reviewed Hino Philippines images are local, optimizable by Next, and selected exclusively through a typed private provenance manifest.
- Content is configurable and avoids unsupported product/local-business claims.
- Quote validation behavior is covered by executable tests and has no lead-delivery pathway.
</success_criteria>

<output>Create `.planning/phases/02-conversion-led-homepage/02-01-SUMMARY.md` when done.</output>
