---
phase: 01-production-contracts-and-executable-configuration
plan: 11
type: execute
wave: 6
depends_on: [01-02, 01-10]
files_modified:
  - src/lib/governance/report.ts
  - tests/review-report.test.mjs
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PROD-06, PROD-07]
must_haves:
  truths:
    - "The allow-listed report DTO includes final safe runtime classifications and final eligible-route output after both subsystems exist."
    - "No raw record, environment value, withheld wording, endpoint, PII, or evidence body enters the DTO."
    - "The DTO exposes D-08 owner-alert status, responsible lane/opaque owner reference, acknowledgement state, and retry/escalation disposition without raw content or provider data."
  artifacts:
    - path: "src/lib/governance/report.ts"
      provides: "Integrated allow-listed review DTO"
      exports: ["getReviewReport"]
  key_links:
    - from: "src/lib/governance/report.ts"
      to: "src/lib/runtime-config.ts"
      via: "safe target/isolation classifications"
      pattern: "getRuntimeConfig|parseRuntimeConfig"
    - from: "src/lib/governance/report.ts"
      to: "src/lib/governance/eligibility.ts"
      via: "final eligible-route result"
      pattern: "getEligibleRoutes"
    - from: "src/lib/governance/report.ts"
      to: "src/content/governance/decisions.ts"
      via: "redacted D-08 owner-alert status projection"
      pattern: "alert|acknowledg|escalat"
---
## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Integrate governance, runtime isolation, and final route eligibility into one redacted report boundary before any report UI consumes it.</objective>
<execution_context>@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-production-contracts-and-executable-configuration/01-02-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-10-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-UI-SPEC.md
@src/lib/runtime-config.ts
@src/lib/governance/eligibility.ts
@src/app/actions/leads.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build and test the integrated redacted DTO</name>
  <files>src/lib/governance/report.ts, tests/review-report.test.mjs</files>
  <behavior>
    - DTO order and vocabulary match UI-SPEC; blockers, approvals, routes, and isolation rows are deterministic.
    - Route rows come from final `getEligibleRoutes`; environment rows come from safe parsed classifications.
    - Recursive key/value inspection finds no raw origins, profile IDs, secrets, PII, endpoints, signed URLs, evidence bodies, or withheld copy.
    - Alert rows expose only stable IDs, lane/opaque owner reference, trigger/status classifications, acknowledgement evidence status, and retry/escalation disposition.
  </behavior>
  <action>Per D-03 and D-05 through D-08, write the redaction/integration tests first using the `tests/foundation.test.mjs` native harness and confirm the intended missing-report failure, then make the final command pass. Follow `src/app/actions/leads.ts` for field-by-field safe result construction and never spread raw records/config. Consume Plan 02's safe classifications, Plan 10's final eligible-route output, and Plan 01's provider-neutral alert records explicitly inside `getReviewReport(now)`. Include only UI-SPEC labels, role lanes/opaque owner references, dates, blocker codes, opaque evidence status/reference, retained-category names, affected surfaces, alert trigger/status classifications, acknowledgement evidence status, retry/escalation disposition, and next actions. Never expose raw governed content, notification-provider payloads, or contact data. Sort exactly as UI-SPEC requires and provide generation-error state without values.</action>
  <verify><automated>node --test tests/review-report.test.mjs &amp;&amp; node --test tests/governance.test.mjs tests/configuration.test.mjs tests/surface-coherence.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>The shared DTO pipeline is explicitly post-runtime/post-route, fully redacted, deterministic, and ready for UI consumption.</done>
</task>
</tasks>
<threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03 | Disclosure | report DTO | mitigate | production consumer guard in next plan |
| T-04 | Disclosure | DTO fields | mitigate | allow-list and recursive forbidden-value tests |
| T-05 | Disclosure | environment rows | mitigate | classifications only |
| T-09 | Disclosure | errors | mitigate | stable codes/names only |</threat_model>
<verification>DTO integration/redaction suite and full check pass.</verification>
<success_criteria>`getReviewReport` cannot drift from runtime isolation or final route eligibility.</success_criteria>
<output>Create `01-11-SUMMARY.md`.</output>
