---
phase: 01-production-contracts-and-executable-configuration
plan: 16
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - src/app/(public)/lp/[slug]/page.tsx
  - src/app/lp/[slug]/page.tsx
  - tests/campaign-surfaces.test.mjs
autonomous: true
requirements: [PROD-03, PROD-05]
must_haves:
  truths:
    - "Campaign routes use eligible claims, final routes, generated params, metadata, and honest request semantics (D-05 through D-12)."
  artifacts:
    - path: "src/app/(public)/lp/[slug]/page.tsx"
      provides: "Eligible campaign route"
    - path: "tests/campaign-surfaces.test.mjs"
      provides: "Campaign route, params, metadata, and request-semantics assertions"
  key_links:
    - from: "src/app/(public)/lp/[slug]/page.tsx"
      to: "src/lib/governance/eligibility.ts"
      via: "one final eligible route result for lookup, params, metadata, and rendering"
      pattern: "generateStaticParams|getEligibleRoutes|notFound"
---
## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Govern the campaign dynamic route and all of its discovery hooks.</objective>
<execution_context>@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-production-contracts-and-executable-configuration/01-04-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@src/app/lp/[slug]/page.tsx
@src/content/campaigns.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Move and govern campaign route behavior</name>
  <files>src/app/(public)/lp/[slug]/page.tsx, src/app/lp/[slug]/page.tsx, tests/campaign-surfaces.test.mjs</files>
  <behavior>
    - Withheld campaign records are absent from generated params, metadata, route rendering, and form semantics.
    - Eligible reduced-form campaigns contain only retained approved elements; unknown or withheld slugs call `notFound()`.
  </behavior>
  <action>Write the campaign owner suite first following `tests/foundation.test.mjs`, then follow the current `generateStaticParams`/`generateMetadata`/`notFound` structure while sourcing lookup, params, metadata, and rendering from one final eligible-route result. Move the declared route into `(public)` without changing the URL and remove exactly the declared old route file. Per D-05 through D-08, omit ineligible claims and metadata; per D-09 through D-12, use only approved request/receipt/contact semantics without implementing Phase 2 delivery. Keep all repair assertions in `tests/campaign-surfaces.test.mjs` limited to this route.</action>
  <verify><automated>node --test --test-name-pattern="campaign|generated params|metadata" tests/campaign-surfaces.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>Campaign content cannot leak through params, metadata, form copy, or route markup.</done>
</task>
</tasks>
<threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02 | Tampering | campaign eligibility | mitigate | one selector for lookup, params, metadata, and render |
| T-04 | Disclosure | campaign metadata/copy | mitigate | eligible fields and owner assertions only |
| T-07 | Repudiation | campaign request semantics | mitigate | approved durable-receipt language only |</threat_model>
<verification>Campaign owner suite and full check pass.</verification>
<success_criteria>PROD-03/05 cover every campaign discovery and request-semantics surface.</success_criteria>
<output>Create `01-16-SUMMARY.md`.</output>
