---
phase: 01-production-contracts-and-executable-configuration
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests/configuration.test.mjs
  - src/lib/runtime-config.ts
  - src/lib/site-url.ts
  - next.config.ts
  - src/app/robots.ts
  - src/app/actions/leads.ts
  - .env.example
autonomous: true
requirements: [PROD-06, PROD-07]
must_haves:
  truths:
    - "Unsafe or inconsistent production configuration fails before deployment and never echoes values."
    - "Development, preview, and production origins, lead profiles, analytics profiles, crawl policies, and review availability are isolated."
    - "Production lead submission remains blocked until the D-09 durable contract is approved."
  artifacts:
    - path: "src/lib/runtime-config.ts"
      provides: "Pure target-aware configuration parser"
      exports: ["parseRuntimeConfig", "getRuntimeConfig"]
    - path: "tests/configuration.test.mjs"
      provides: "T-03 through T-09 target matrix"
  key_links:
    - from: "next.config.ts"
      to: "src/lib/runtime-config.ts"
      via: "pre-export validation"
      pattern: "parseRuntimeConfig|getRuntimeConfig"
---

## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Make PROD-06/07 configuration and environment isolation executable and fail closed.</objective>
<execution_context>@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/01-production-contracts-and-executable-configuration/01-01-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-VALIDATION.md
@next.config.ts
@src/lib/site-url.ts
@src/app/robots.ts
@src/app/actions/leads.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the target parser, pre-build gate, and isolation matrix</name>
  <files>tests/configuration.test.mjs, src/lib/runtime-config.ts, src/lib/site-url.ts, next.config.ts, src/app/robots.ts, .env.example</files>
  <behavior>
    - Development accepts local origin and disabled/development profiles only; preview requires unique HTTPS, blocked crawl, and sandbox/test-or-disabled profiles; production requires exact approved HTTPS origin and production-safe profiles.
    - Credentials, path/query/hash, localhost production, target mismatch, and raw-value error leakage are rejected.
    - Invalid production build smoke exits non-zero for a stable `CFG_*` reason before successful compilation.
  </behavior>
  <action>Per D-03, D-05, D-06, D-12, and D-13, write the environment matrix first using the `tests/foundation.test.mjs` native source-contract pattern and confirm its intended missing-parser failure, then complete implementation so the final verification passes. Follow `src/app/actions/leads.ts` for Zod shape construction and value-safe errors, `src/lib/site-url.ts` for the centralized `getSiteOrigin`/`absoluteUrl` API, and the existing `next.config.ts` typed export/security-header pattern. Pick allow-listed environment keys instead of spreading `process.env`; cross-check `DEPLOYMENT_ENV` with `VERCEL_ENV`; return classifications and a non-secret fingerprint only. Preserve development localhost behavior but never permit fallback in preview/production. Document variable names and safe classes only.</action>
  <verify><automated>node --test --test-name-pattern="production|isolation" tests/configuration.test.mjs &amp;&amp; npm run typecheck</automated></verify>
  <done>The full PROD-06/07 matrix passes and invalid production configuration blocks before deployment without leaking values.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Gate robots and lead use with parsed target readiness</name>
  <files>src/app/robots.ts, src/app/actions/leads.ts, tests/configuration.test.mjs</files>
  <behavior>
    - Non-production always blocks crawl; production allows only an approved crawl decision.
    - Production submission returns the existing honest retry/contact failure before `leadRouter.submit` unless the current durable contract is approved.
  </behavior>
  <action>Follow the existing `src/app/robots.ts` `MetadataRoute.Robots` boundary and `src/app/actions/leads.ts` early-return error pattern. Consume parsed classes and approved lead capability predicates; do not implement the Phase 2 provider, queue, retry engine, or success semantics. Add passing source/behavior assertions for the guard ordering.</action>
  <verify><automated>node --test tests/configuration.test.mjs &amp;&amp; node --test tests/governance.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>Crawl and production mutation boundaries use the same safe runtime classification and D-09/D-12 readiness contract.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04 | Disclosure | configuration | mitigate | key allow-list and value-free errors |
| T-05 | Tampering/Disclosure | target profiles | mitigate | closed target matrix |
| T-06 | SSRF/Tampering | origins | mitigate | exact HTTPS origin validation |
| T-07 | Repudiation | lead action | mitigate | readiness guard before router |
| T-08 | Availability | fingerprint | mitigate | non-secret reconciliation fingerprint |
| T-09 | Disclosure | build errors | mitigate | stable codes/names only |
</threat_model>
<verification>`npm run check` passes; deliberate invalid configuration fails safely.</verification>
<success_criteria>Production cannot build or accept inquiries under unsafe configuration.</success_criteria>
<output>Create `01-02-SUMMARY.md` in this phase directory.</output>
