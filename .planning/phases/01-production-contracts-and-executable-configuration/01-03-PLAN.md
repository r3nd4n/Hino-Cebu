---
phase: 01-production-contracts-and-executable-configuration
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/content/governance/claims.ts
  - src/lib/governance/eligibility.ts
  - src/content/site.ts
  - src/content/trucks.ts
  - src/content/campaigns.ts
  - src/content/services.ts
  - tests/governance.test.mjs
autonomous: true
requirements: [PROD-02, PROD-03]
must_haves:
  truths:
    - "Branch field groups and claims are independently eligible only with correct lane, evidence, Cebu applicability, freshness, and active revision (D-02 through D-05, D-08)."
    - "Routes expose a final Eligible, Eligible in reduced form, or Withheld result from the four-part minimum truth contract (D-07)."
  artifacts:
    - path: "src/content/site.ts"
      provides: "Canonical branch record and eligible contact selector"
      exports: ["getEligibleBranch", "getEligibleContactActions"]
    - path: "src/lib/governance/eligibility.ts"
      provides: "Claim and final route selectors"
      exports: ["getEligibleClaims", "getEligibleRoutes"]
  key_links:
    - from: "src/lib/governance/eligibility.ts"
      to: "src/content/governance/claims.ts"
      via: "validated claim evaluation"
      pattern: "evaluateApproval|getEligibleClaims"
---

## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Create the canonical branch, claim, and final route eligibility APIs consumed by every later public surface.</objective>
<execution_context>@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/01-production-contracts-and-executable-configuration/01-01-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@SOURCE_REGISTER.md
@BUSINESS_INPUTS_REQUIRED.md
@src/content/promotions.ts
@src/content/site.ts
@src/content/trucks.ts
@src/content/campaigns.ts
@src/content/services.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic branch, claim, and route selectors with passing matrices</name>
  <files>src/content/governance/claims.ts, src/lib/governance/eligibility.ts, src/content/site.ts, src/content/trucks.ts, src/content/campaigns.ts, src/content/services.ts, tests/governance.test.mjs</files>
  <behavior>
    - `reviewAt === now` is due; pending, expired, invalidated, wrong-lane, wrong-locality, malformed-evidence, and superseded data is omitted.
    - Branch identity/address/phone/hours/directions/actions are independently eligible.
    - A route returns one final eligibility state plus retained/withheld categories without exposing withheld wording.
  </behavior>
  <action>Write the selector matrix first, confirm the intended missing-selector failure, then make it pass. Follow `src/content/promotions.ts` for named exports, early returns, and injected `now`; preserve `src/content/site.ts` as the sole branch owner; use the Zod contracts from Plan 01. Associate truck, campaign, and service entries with stable claim/route IDs. Preserve only source-backed facts with accurate pending/approved status; never infer Cebu approval from national sources or current markup. Implement D-05 honest-task retention, D-07 four checks, and D-08 immediate removal/unavailable-page rules as final selector output.</action>
  <verify><automated>node --test --test-name-pattern="branch|eligibility|route|owner alert" tests/governance.test.mjs &amp;&amp; npm run typecheck</automated></verify>
  <done>PROD-02/03 selectors pass positive, reduced-form, withheld, due, and invalidated cases and expose no raw approval collections to public consumers.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01 | Elevation | lanes | mitigate | fixed-lane selector tests |
| T-02 | Tampering | claims/routes | mitigate | evidence, revision, time, locality gates |
| T-04 | Disclosure | selector output | mitigate | allow-listed output without withheld text |
| T-06 | SSRF | directions/evidence | mitigate | approved URL shape; no evidence fetch |
</threat_model>
<verification>Focused selector tests and `npm run check` pass.</verification>
<success_criteria>All downstream surfaces have one deterministic approved-content and route eligibility contract.</success_criteria>
<output>Create `01-03-SUMMARY.md` in this phase directory.</output>
