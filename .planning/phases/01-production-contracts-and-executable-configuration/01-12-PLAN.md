---
phase: 01-production-contracts-and-executable-configuration
plan: 12
type: execute
wave: 7
depends_on: [01-04, 01-11]
files_modified:
  - src/app/review/approvals/page.tsx
  - src/app/globals.css
  - tests/report-ui.test.mjs
  - .planning/phases/01-production-contracts-and-executable-configuration/01-VALIDATION.md
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PROD-06, PROD-07]
must_haves:
  truths:
    - "The protected preview report is shell-free, server-rendered, read-only, redacted, accessible, noindex/nofollow/noarchive, and hard-404s before DTO construction in production (D-06)."
    - "The report renders the exact UI-SPEC order, copy, vocabulary, responsive behavior, and limitations."
  artifacts:
    - path: "src/app/review/approvals/page.tsx"
      provides: "Preview-only approval report"
    - path: "tests/report-ui.test.mjs"
      provides: "Report boundary, content, and accessibility source contracts"
  key_links:
    - from: "src/app/review/approvals/page.tsx"
      to: "src/lib/runtime-config.ts"
      via: "production notFound before report DTO"
      pattern: "notFound.*getReviewReport"
    - from: "src/app/review/approvals/page.tsx"
      to: "src/lib/governance/report.ts"
      via: "allow-listed DTO only"
      pattern: "getReviewReport"
---
## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Render the exact protected approval report contract and finalize executable Nyquist mappings.</objective>
<execution_context>@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-production-contracts-and-executable-configuration/01-04-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-11-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-UI-SPEC.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-VALIDATION.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@src/app/globals.css
@src/app/lp/[slug]/page.tsx</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the protected report boundary and exact UI contract</name>
  <files>src/app/review/approvals/page.tsx, src/app/globals.css, tests/report-ui.test.mjs</files>
  <behavior>
    - Production guard executes before `getReviewReport`; production returns shared shell-free 404 with no report title/DTO.
    - Preview metadata is noindex/nofollow/noarchive and source has no client hooks, forms, mutation/provider/upload/copy/promote controls.
    - Exact seven-section order, six anchor links, required status/copy vocabulary, semantic tables/cards, and 1060/720 responsive contracts are present.
    - D-08 alert status appears only inside the existing applicable approvals/blockers section, using lane/opaque owner reference, acknowledgement state, and retry/escalation disposition without adding a section or exposing raw content.
  </behavior>
  <action>Per D-06, write source/DTO-render contract tests first using the `tests/foundation.test.mjs` native source-contract pattern and confirm the intended missing-page failure, then make the final command pass. Follow `src/app/lp/[slug]/page.tsx` for server-only metadata/notFound flow; call `notFound()` from the safe target classification before invoking `getReviewReport`. Render one `main#main-content`, UI-SPEC's fixed information order and exact copy, visible captions/scoped headers plus mobile definition-card equivalents. Extend only scoped classes in `globals.css`, preserving its existing tokens, 1060/720 breakpoints, focus rules, reduced-motion rule, and public class semantics. Use four sizes/two weights, six/three/one summary columns, 44px targets, overflow wrapping, no animation, no horizontal scrolling, and no sensitive/raw values.</action>
  <verify><automated>node --test tests/report-ui.test.mjs tests/review-report.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>The report satisfies every UI-SPEC, discovery, redaction, production-absence, responsive, and accessibility contract.</done>
</task>
<task type="auto">
  <name>Task 2: Finalize the validation map only after all executable tests exist</name>
  <files>.planning/phases/01-production-contracts-and-executable-configuration/01-VALIDATION.md</files>
  <action>Replace the provisional Wave 0/TBD rows with a per-task map covering every task in Plans 01-01 through 01-16: `01-01-1` (wave 1); `01-02-1` and `01-02-2` (wave 2); `01-03-1` (wave 2); `01-04-1` and `01-04-2` (wave 3); `01-05-1`, `01-06-1`, `01-07-1`, `01-08-1`, `01-09-1`, `01-15-1`, and `01-16-1` (wave 4); `01-10-1` (wave 5); `01-11-1` (wave 6); `01-12-1` and `01-12-2` (wave 7); `01-13-1` (wave 3); and checkpoint tasks `01-14-1` and `01-14-2` (wave 8). For every row record the actual plan, wave, requirements/threats, the exact `<verify><automated>` command copied from its owning PLAN, and whether each command segment is the task-owned focused test or final/regression smoke (`npm run check` and cross-owner suites). Mark `01-14-1` as a blocking human-action evidence gate and `01-14-2` as a blocking human-verification Vercel/protection/authority gate while retaining their automated regression commands; include the Plan 12 visual checks at 320px, 800px, desktop, keyboard, headings/captions, and 200% zoom as a manual human check rather than automation. Keep T-01 through T-09 and PROD-01 through PROD-07 traceability, preserve external Vercel/evidence/authority checks as manual gates, and do not claim they are automated. Set `wave_0_complete: true` and `nyquist_compliant: true` only after all 20 task rows, their owner/final-smoke classifications, every referenced test file/assertion, and the complete automated/manual gate map are verified; otherwise leave both flags false.</action>
  <verify><automated>node --test tests/governance.test.mjs tests/configuration.test.mjs tests/branch-surfaces.test.mjs tests/sales-surfaces.test.mjs tests/editorial-surfaces.test.mjs tests/delivery-surfaces.test.mjs tests/legal-aftersales-surfaces.test.mjs tests/product-surfaces.test.mjs tests/campaign-surfaces.test.mjs tests/surface-coherence.test.mjs tests/review-report.test.mjs tests/report-ui.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>Nyquist coverage truthfully maps all 20 tasks across Plans 01-01 through 01-16, including manual/checkpoint gates and owner-versus-final-smoke classification, before either completion flag becomes true.</done>
</task>
</tasks>
<threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03 | Disclosure | report route | mitigate | platform protection prerequisite, production guard before DTO, discovery absence |
| T-04 | Disclosure | rendered output | mitigate | allow-listed DTO and forbidden-field tests |
| T-09 | Disclosure | report errors | mitigate | exact safe failure copy |</threat_model>
<verification>All focused suites and `npm run check` pass; manual visual checks cover 320px, 800px, desktop, keyboard, headings, captions, and 200% zoom.</verification>
<success_criteria>The only new UI is a protected, exact, accessible report and production reveals no route/report data.</success_criteria>
<output>Create `01-12-SUMMARY.md`.</output>
