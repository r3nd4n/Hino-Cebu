---
phase: 01-production-contracts-and-executable-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/governance/schemas.ts
  - src/content/governance/decisions.ts
  - src/content/governance/privacy.ts
  - src/content/governance/leads.ts
  - src/content/governance/release.ts
  - tests/governance.test.mjs
  - tests/fixtures/governance/records.mjs
  - tests/fixtures/governance/tsconfig.json
autonomous: true
requirements: [PROD-01, PROD-03, PROD-04, PROD-05]
must_haves:
  truths:
    - "The repository represents two-tier approval, fixed lanes, evidence references, review/invalidation, and release authority without storing sensitive evidence (D-01 through D-04, D-13 through D-16)."
    - "Every lead type has one provider-neutral pending operating contract that encodes durable receipt, ownership, escalation, recovery, and equivalent secondary intake (D-09 through D-12)."
    - "Unknown business, legal, provider, owner, and threshold values remain explicit production blockers."
    - "Expiry or invalidation creates a provider-neutral, redacted owner-alert record with a responsible lane, acknowledgement evidence, and retry/escalation disposition (D-08)."
  artifacts:
    - path: "src/lib/governance/schemas.ts"
      provides: "Zod runtime contracts and inferred closed-domain types"
      exports: ["approvalSchema", "evidenceReferenceSchema", "governanceRegistrySchema"]
    - path: "tests/governance.test.mjs"
      provides: "T-01/T-02/T-04/T-06/T-07 record contract tests"
  key_links:
    - from: "src/content/governance/*.ts"
      to: "src/lib/governance/schemas.ts"
      via: "module-boundary Zod parsing"
      pattern: "Schema\\.parse|safeParse"
---

## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Create the typed, runtime-validated governance records that are authoritative for Phase 1.</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-CONTEXT.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-RESEARCH.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@src/content/promotions.ts
@src/lib/leads/types.ts
@src/app/actions/leads.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement approval schemas and fixed-lane records with passing contract tests</name>
  <files>src/lib/governance/schemas.ts, src/content/governance/decisions.ts, src/content/governance/privacy.ts, src/content/governance/leads.ts, src/content/governance/release.ts, tests/governance.test.mjs, tests/fixtures/governance/records.mjs, tests/fixtures/governance/tsconfig.json</files>
  <behavior>
    - Pending, missing-evidence, wrong-lane, due-at-now, expired, invalidated, malformed-reference, and superseded records block; approved/current records pass.
    - Exactly one contract exists for sales, financing, fleet, service, and parts, with D-10 department ownership and D-11 two-stage escalation.
    - Proposal values cannot satisfy an approved policy predicate.
    - Expiry/invalidation emits one stable alert event containing only alert ID, governed record ID/revision, responsible lane/owner reference, trigger code/time, status, acknowledgement evidence reference, attempt state, and retry/escalation disposition; raw content and sensitive evidence are rejected.
  </behavior>
  <action>Write the assertions first, run them to confirm the intended missing-contract failure, then implement until the final command passes. Follow `tests/foundation.test.mjs` for native `node:test` organization, `src/content/promotions.ts` for named typed repository records plus injected-time selection, `src/app/actions/leads.ts` for Zod parsing and safe failure translation, and `src/lib/leads/types.ts` for the exact `LeadType` union. Define stable IDs/revisions, fixed D-02 lanes, D-03 role/date/review/evidence fields, D-04 invalidation/supersession, D-09/D-10/D-11/D-12 durable capability predicates, D-13 ordinary promotion, D-14 emergency authority, D-15 rollback authority, and D-16 closeout fields. Per D-08, define a bounded provider-neutral owner-alert event/record contract and pure expiry/invalidation trigger: stable alert/event and governed-record IDs, record revision, responsible approval lane plus opaque owner reference, trigger code/time, acknowledgement status/time/evidence reference, attempt state, and retry/escalation disposition. Reject raw content, withheld wording, PII, names, credentials, endpoints, evidence bodies, and free-form provider payloads; do not select or deliver through a notification provider. Seed unknown real values as `pending` or `proposal`; use only synthetic `example.invalid` fixtures and opaque `EVID-*` references. This task also implements D-01 two-tier approval explicitly.</action>
  <verify><automated>node --test --test-name-pattern="production estate|privacy|lead contract|approval|owner alert" tests/governance.test.mjs &amp;&amp; npm run typecheck</automated></verify>
  <done>PROD-01/04/05 record shapes parse, negative approval cases fail closed, and all unknown real values remain blockers per D-01 through D-04 and D-09 through D-16.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01 | Spoofing/Elevation | approval lanes | mitigate | fixed roles, two-tier confirmation, wrong-lane tests |
| T-02 | Tampering | records | mitigate | revisions, evidence refs, temporal and invalidation tests |
| T-04 | Disclosure | repository/fixtures | mitigate | opaque refs, deny sensitive fields, synthetic data |
| T-07 | Repudiation | lead contract | mitigate | durable acceptance capabilities required before approval |
</threat_model>

<verification>Focused tests and `npm run check` pass; no new package is installed.</verification>
<success_criteria>The repository has one executable non-sensitive record contract for PROD-01, PROD-04, and PROD-05.</success_criteria>
<output>Create `.planning/phases/01-production-contracts-and-executable-configuration/01-01-SUMMARY.md`.</output>
