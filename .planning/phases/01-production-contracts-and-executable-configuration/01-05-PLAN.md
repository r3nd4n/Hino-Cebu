---
phase: 01-production-contracts-and-executable-configuration
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - src/app/(public)/contact/page.tsx
  - src/app/(public)/hino-cebu/page.tsx
  - src/app/contact/page.tsx
  - src/app/hino-cebu/page.tsx
  - tests/branch-surfaces.test.mjs
autonomous: true
requirements: [PROD-02, PROD-03]
must_haves:
  truths:
    - "Contact and branch pages render one independently eligible branch record and honest actions only."
  artifacts:
    - path: "src/app/(public)/contact/page.tsx"
      provides: "Governed contact route"
    - path: "src/app/(public)/hino-cebu/page.tsx"
      provides: "Governed branch route"
  key_links:
    - from: "branch routes"
      to: "src/content/site.ts"
      via: "eligible branch/action selectors"
      pattern: "getEligibleBranch|getEligibleContactActions"
---
## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Move and govern the branch/contact public routes without changing URLs.</objective>
<execution_context>@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-production-contracts-and-executable-configuration/01-04-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@src/app/contact/page.tsx
@src/app/hino-cebu/page.tsx
@src/content/site.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Move and migrate branch-facing pages</name>
  <files>src/app/(public)/contact/page.tsx, src/app/(public)/hino-cebu/page.tsx, src/app/contact/page.tsx, src/app/hino-cebu/page.tsx, tests/branch-surfaces.test.mjs</files>
  <behavior>
    - Existing `/contact` and `/hino-cebu` paths remain unchanged.
    - Pending hours/directions/actions are omitted independently; verified minimum facts remain understandable.
  </behavior>
  <action>Follow `src/app/lp/[slug]/page.tsx` for declarative server routes and metadata factories and `tests/foundation.test.mjs` for the new native source-contract test; preserve each page's current composition. Move the routes into `(public)` and remove the old route files. Replace direct branch constants and embedded schema values with `getEligibleBranch`/`getEligibleContactActions`. Per D-05 through D-08, omit ineligible values and retain only approved truthful request/contact actions; render no approval diagnostics or provisional text. Add passing route/source assertions.</action>
  <verify><automated>node --test --test-name-pattern="branch|contact" tests/branch-surfaces.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>Both URLs preserve their jobs while public and structured branch facts come from one eligible record.</done>
</task>
</tasks>
<threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02 | Tampering | branch pages | mitigate | canonical selector and omission tests |
| T-06 | SSRF | directions links | mitigate | eligible approved URL only |</threat_model>
<verification>Focused tests and full check pass.</verification>
<success_criteria>PROD-02 is enforced on contact and branch pages.</success_criteria>
<output>Create `01-05-SUMMARY.md`.</output>
