---
phase: 01-production-contracts-and-executable-configuration
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/app/layout.tsx
  - src/app/(public)/layout.tsx
  - src/app/not-found.tsx
  - src/app/(public)/not-found.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/StickyMobileActions.tsx
  - tests/governance.test.mjs
autonomous: true
requirements: [PROD-02, PROD-03, PROD-07]
must_haves:
  truths:
    - "The public shell is structurally isolated from review routes without URL or client-pathname tricks."
    - "Header, footer, and mobile actions contain only eligible routes and eligible branch/contact actions (D-05 through D-08)."
  artifacts:
    - path: "src/app/(public)/layout.tsx"
      provides: "Public-only shell"
    - path: "src/app/layout.tsx"
      provides: "Minimal document boundary"
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "src/lib/governance/eligibility.ts"
      via: "server-side evaluation of eligible navigation/actions"
      pattern: "getEligibleRoutes|getEligibleContactActions"
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "allow-listed eligible navigation/contact DTO props"
      pattern: "Header.*navigation|Header.*contact"
    - from: "src/app/(public)/layout.tsx"
      to: "src/content/site.ts"
      via: "organization JSON-LD built only from getEligibleBranch output"
      pattern: "getEligibleBranch"
    - from: "src/app/(public)/not-found.tsx"
      to: "final public selectors"
      via: "eligible recovery links and contact actions"
      pattern: "getEligibleRoutes|getEligibleContactActions"
---
## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Separate the public shell and migrate global navigation/action consumers to eligible selectors.</objective>
<execution_context>@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-production-contracts-and-executable-configuration/01-02-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-03-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@src/app/layout.tsx
@src/components/layout/Header.tsx
@src/components/layout/Footer.tsx
@src/components/layout/StickyMobileActions.tsx</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create the route-group shell boundary</name>
  <files>src/app/layout.tsx, src/app/(public)/layout.tsx, src/app/not-found.tsx, src/app/(public)/not-found.tsx, tests/governance.test.mjs</files>
  <behavior>
    - Organization JSON-LD in the public layout is constructed only from `getEligibleBranch`; pending/expired branch fields never enter schema output.
    - The grouped public 404 derives recovery links from `getEligibleRoutes` and contact actions from `getEligibleContactActions`; the root 404 remains shell-free.
  </behavior>
  <action>Write source-contract assertions first in `tests/governance.test.mjs`, then follow the existing `src/app/layout.tsx` server layout pattern while making the root layout only the html/body/global-CSS boundary. Put Header/main/Footer/StickyMobileActions/Suspense/AttributionCapture/MarketingTags and organization JSON-LD in `(public)/layout.tsx`; explicitly construct organization JSON-LD from `getEligibleBranch` rather than raw site records. Preserve a safe shell-free root 404 and make the public grouped 404 render only final `getEligibleRoutes` recovery links plus `getEligibleContactActions` actions. Per D-05 through D-08, assert synthetic withheld routes/actions and expired branch fields are absent from both files. Per D-06, do not use `use client`, pathname inspection, CSS hiding, redirects, or application authentication. Keep public URLs unchanged as later route plans move pages into the group.</action>
  <verify><automated>node --test --test-name-pattern="public layout|organization|public 404" tests/governance.test.mjs &amp;&amp; npm run typecheck</automated></verify>
  <done>The review subtree cannot inherit public shell or marketing integrations and existing routes still compile.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Migrate navigation and contact action registries</name>
  <files>src/app/(public)/layout.tsx, src/components/layout/Header.tsx, src/components/layout/Footer.tsx, src/components/layout/StickyMobileActions.tsx, tests/governance.test.mjs</files>
  <behavior>
    - Withheld routes and expired/invalidated contact actions are absent from every global shell surface.
    - Public shell source contains no review labels, provisional copy, or diagnostics.
    - Client Components import neither raw governance records nor `getEligibleBranch`, `getEligibleRoutes`, `getEligibleClaims`, or `getEligibleContactActions`.
  </behavior>
  <action>Follow each existing component's local server/client boundary and direct-import style. In the Server Component `src/app/(public)/layout.tsx`, evaluate `getEligibleBranch`, `getEligibleRoutes`, and `getEligibleContactActions` per D-05 through D-08, project their results into explicit allow-listed navigation/contact DTO props, and pass only those DTOs into `Header` and any other client island. Keep governance evaluation, raw governance records, approval metadata, and selector imports out of `Header.tsx` and every file declaring `"use client"`; server-compatible shell components may receive the same eligible DTOs without reevaluating raw records. Preserve keyboard/mobile behavior and `TrackedLink` analytics without adding diagnostic state. Add passing source-contract assertions that scan Client Components and fail if they import governance records or any eligibility selector.</action>
  <verify><automated>node --test --test-name-pattern="navigation|surface" tests/governance.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>All global public navigation/link/action registries fail closed through server-evaluated canonical selectors, and no Client Component imports governance records or eligibility selectors.</done>
</task>
</tasks>
<threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03 | Disclosure | review boundary | mitigate | structural route group and shell-free root 404 |
| T-02 | Tampering | navigation | mitigate | final eligible-route selector only |
| T-04 | Disclosure | public shell | mitigate | no report DTO or diagnostics in public components |</threat_model>
<verification>`npm run check` passes and shell source has no review/provisional diagnostics.</verification>
<success_criteria>Public and protected-review layouts are structurally separate and global discovery links are eligible-only.</success_criteria>
<output>Create `01-04-SUMMARY.md`.</output>
