---
phase: 01-production-contracts-and-executable-configuration
plan: 15
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - src/app/(public)/hino-cebu/customer-deliveries/page.tsx
  - src/app/hino-cebu/customer-deliveries/page.tsx
  - src/content/deliveries.ts
  - src/content/businessApplications.ts
  - tests/delivery-surfaces.test.mjs
autonomous: true
requirements: [PROD-03]
must_haves:
  truths:
    - "Delivery stories and business-application claims cannot expose unapproved local facts, customer material, assets, or testimonials (D-05 through D-08)."
  artifacts:
    - path: "src/content/deliveries.ts"
      provides: "Governed delivery content"
    - path: "tests/delivery-surfaces.test.mjs"
      provides: "Delivery and application owner assertions"
  key_links:
    - from: "src/app/(public)/hino-cebu/customer-deliveries/page.tsx"
      to: "src/lib/governance/eligibility.ts"
      via: "eligible claims and final route output"
      pattern: "getEligibleClaims|getEligibleRoutes"
---
## Phase Goal

**As a** release operator, **I want to** enforce approved business, legal, account, routing, and ownership contracts, **so that** production can operate safely.

<objective>Apply fail-closed governance to customer-delivery and business-application content.</objective>
<execution_context>@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-production-contracts-and-executable-configuration/01-04-SUMMARY.md
@.planning/phases/01-production-contracts-and-executable-configuration/01-PATTERNS.md
@src/content/deliveries.ts
@src/content/businessApplications.ts
@src/app/hino-cebu/customer-deliveries/page.tsx</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Govern delivery and application content and move the delivery route</name>
  <files>src/app/(public)/hino-cebu/customer-deliveries/page.tsx, src/app/hino-cebu/customer-deliveries/page.tsx, src/content/deliveries.ts, src/content/businessApplications.ts, tests/delivery-surfaces.test.mjs</files>
  <behavior>
    - Ineligible deliveries, customer materials, and business-application claims are absent; truthful empty states remain.
    - Expiry/invalidation removes listings immediately and withheld wording or asset references never render.
  </behavior>
  <action>Write the native source-contract suite first following `tests/foundation.test.mjs`, then strengthen delivery and business-application records with stable claim IDs and canonical selectors rather than raw approval booleans. Move the customer-deliveries route into `(public)` without changing its URL and remove exactly the declared old route file. Apply D-05 through D-08 to customer material, testimonial-like copy, local application claims, and asset rights; never infer approval or expose review diagnostics. Keep assertions in `tests/delivery-surfaces.test.mjs` limited to the files this plan owns.</action>
  <verify><automated>node --test --test-name-pattern="delivery|application|expired" tests/delivery-surfaces.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>Delivery and application content fails closed and the route preserves an honest usable empty state.</done>
</task>
</tasks>
<threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02 | Tampering | delivery/application records | mitigate | stable claim IDs and freshness/invalidation selectors |
| T-04 | Disclosure | customer material/assets | mitigate | selector-only output and owner assertions |</threat_model>
<verification>Delivery owner suite and full check pass.</verification>
<success_criteria>No delivery or business-application surface bypasses PROD-03.</success_criteria>
<output>Create `01-15-SUMMARY.md`.</output>

