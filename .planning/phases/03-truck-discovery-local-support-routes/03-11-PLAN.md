---
phase: 03-truck-discovery-local-support-routes
plan: "11"
type: execute
wave: 8
depends_on: [03-10]
files_modified:
  - src/content/site.ts
  - src/components/contact/ContactEmail.tsx
  - src/app/contact/page.tsx
  - tests/support-routes.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-03]
must_haves:
  truths:
    - "An unresolved or invalid configured email exposes only the awaiting-confirmation row and no mailto link."
    - "Approving a valid configured email reveals exactly that address as a working mailto action without revealing other pending facts."
  artifacts:
    - path: src/content/site.ts
      provides: Status-discriminated public email projection and safe mailto contract.
    - path: src/components/contact/ContactEmail.tsx
      provides: Independently renderable approved/unresolved email treatment.
    - path: src/app/contact/page.tsx
      provides: Contact route wiring to the projected email state.
    - path: tests/support-routes.test.mjs
      provides: Executable projection and server-rendered approved/unresolved email regressions.
  key_links:
    - from: src/content/site.ts
      to: src/components/contact/ContactEmail.tsx
      via: PublicContact email discriminant
      pattern: "email\.status === \"approved\""
    - from: src/app/contact/page.tsx
      to: src/components/contact/ContactEmail.tsx
      via: publicContact.email prop
      pattern: "<ContactEmail email=\{publicContact\.email\}"
---

## Phase Goal

**As a** Cebu business visitor, **I want to** see only confirmed local contact methods, **so that** I can use an approved email address without being shown fabricated or pending facts.

<objective>
Reconnect the authoritative email configuration to the safe public projection and Contact rendering boundary.

Purpose: close the approved-email blocker while preserving the verified-only local-fact contract from Plans 03-05 and 03-07.
Output: a validated public email discriminant, an approved/unresolved renderer, Contact wiring, and executable projection/render coverage.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md
@.planning/phases/03-truck-discovery-local-support-routes/03-05-SUMMARY.md
@.planning/phases/03-truck-discovery-local-support-routes/03-07-SUMMARY.md

<interfaces>
From src/content/site.ts:
- ConfiguredValue&lt;T&gt; carries value, status, and optional launchNote.
- PublicContact currently projects phone, address, directions, and hours as approved-or-awaiting discriminated unions.
- projectPublicContact(config: LocalFactConfiguration): PublicContact is the only supported configured-fact to public-output boundary.
- siteConfig.contact.email is ConfiguredValue&lt;string | null&gt; and defaults unresolved/null.

From src/app/contact/page.tsx:
- ContactPage normalizes searchParams.topic and reads only publicContact for public local facts.
- The current email row is hard-coded as `Email: awaiting confirmation`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define approved and unresolved email projection/render contracts</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md</file>
    <file>src/content/site.ts</file>
    <file>src/app/contact/page.tsx</file>
    <file>tests/support-routes.test.mjs</file>
  </read_first>
  <files>tests/support-routes.test.mjs</files>
  <behavior>
    - Default unresolved email projects `{ status: "awaiting-confirmation" }`, contains no configured candidate, and renders `Email: awaiting confirmation` without an anchor.
    - A valid non-empty email with status `approved` projects `{ status: "approved", display, href: "mailto:..." }` independently of phone/address/hours/directions and renders one matching mailto anchor.
    - Null, blank, malformed, control-character, query-bearing, or fragment-bearing approved values fail closed to awaiting confirmation and never create an href.
  </behavior>
  <action>Extend the existing support test fixture to accept independent email value/status inputs and write the failing projection cases above. Add an actual server-render assertion for both discriminants: keep `ContactEmail.tsx` self-contained except for its type-only PublicContact import, compile that focused source in the test with the already-installed `typescript.transpileModule` using CommonJS plus `react-jsx`, load the temporary compiled module from a test-owned directory, render it with `react-dom/server.renderToStaticMarkup`, and remove the temporary artifact in cleanup. Do not replace this with a source-string assertion and do not add a transpiler/test package. Keep source scans only for the existing static provenance/direct-import boundaries. Per D-18 and D-19, the unresolved branch must be plain status text and the approved branch must use only the projected value.</action>
  <verify>
    <automated>node --test tests/support-routes.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The new tests fail because PublicContact has no email discriminant and no status-driven renderer exists.</criterion>
    <criterion>The render test exercises actual React output for both approved and awaiting states.</criterion>
    <criterion>The fixture proves email approval does not reveal phone, address, hours, or directions.</criterion>
  </acceptance_criteria>
  <done>A failing executable contract captures safe email projection plus rendered approved/unresolved behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project and render only a safe approved email</name>
  <read_first>
    <file>src/content/site.ts</file>
    <file>src/app/contact/page.tsx</file>
    <file>tests/support-routes.test.mjs</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-05-SUMMARY.md</file>
  </read_first>
  <files>src/content/site.ts, src/components/contact/ContactEmail.tsx, src/app/contact/page.tsx</files>
  <behavior>
    - Approved valid configuration yields a mailto action whose visible address and target are identical to the normalized configured address.
    - Every non-approved or unsafe value yields only the binding awaiting-confirmation copy.
  </behavior>
  <action>Add `ApprovedEmail` and `email` to PublicContact and LocalFactConfiguration, then project it independently in `projectPublicContact()`. Trim the configured value and accept it only when status is `approved`, it matches the existing conservative email shape, and it contains no control characters, whitespace, `?`, or `#`; otherwise return `awaitingConfirmation()` so configuration cannot inject mailto headers or a second target. Create a focused `ContactEmail` component with an `email: PublicContact["email"]` prop that returns a native `mailto:` anchor for the approved branch and the exact UI-SPEC pending row otherwise. Wire ContactPage to `&lt;ContactEmail email={publicContact.email} /&gt;`. Do not add a fallback address, env read, client import, network call, or Phase 4 behavior. This implements D-18/D-19 and preserves the independent projection established by Plan 03-05.</action>
  <verify>
    <automated>node --test tests/support-routes.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Default production Contact output still contains `Email: awaiting confirmation` and no `mailto:` link.</criterion>
    <criterion>The approved fixture renders exactly one safe `mailto:` anchor with the configured display value.</criterion>
    <criterion>Unsafe approved values fail closed and no other configured fact changes public status.</criterion>
    <criterion>No pending phone/address/map/hours/directions value, provider behavior, Promotions surface, or mother-site link is introduced.</criterion>
  </acceptance_criteria>
  <done>Email now follows the same truthful, independently approval-gated projection and rendering contract as the other local facts.</done>
</task>

</tasks>

<source_coverage>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Extend conversion across Contact/local-dealer information | 03-11 | COVERED | Approved email becomes usable without weakening safe defaults. |
| REQ | DISC-03 | Dedicated Contact route with clear next actions | 03-11 | COVERED | Status-driven email is a real action only when approved. |
| CONTEXT | D-18 | Show verified Contact facts; fabricate no email/link | 03-11 | COVERED | Projection validates before constructing mailto. |
| CONTEXT | D-19 | Mark unresolved email awaiting confirmation | 03-11 | COVERED | Binding pending copy remains non-interactive. |
| RESEARCH | - | Honest unresolved-state rendering and typed public projection | 03-11 | COVERED | Email joins the established discriminated projection. |
| UI-SPEC | - | Email pending row, native actions, no inactive anchors | 03-11 | COVERED | Both rendered discriminants are tested. |
| REVIEW | CR-01 | Approved email discarded by projection | 03-11 | COVERED | Projection, wiring, and render tests close the blocker. |
</source_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authoritative contact configuration to public projection | Maintainer-provided email becomes public only after approval and syntax safety checks. |
| public projection to Contact markup | Only the discriminated public value may become a live mailto action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-41 | Tampering | projectPublicContact email branch | mitigate | Reject control/query/fragment-bearing or malformed configured email before constructing href. |
| T-03-42 | Information Disclosure | ContactEmail unresolved branch | mitigate | Awaiting discriminant carries and renders no raw configured value. |
| T-03-43 | Spoofing | approved Contact email action | mitigate | Derive display and mailto target from the same validated projected address. |
| T-03-SC | Tampering | package installs | accept | No package installation occurs; existing pinned React/TypeScript tooling supplies render coverage. |
</threat_model>

<verification>
<automated>node --test tests/support-routes.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
</verification>

<success_criteria>
- Approved email crosses the public projection and renders one safe mailto action.
- Unresolved and unsafe email values render only the exact awaiting-confirmation state.
- Projection and actual rendered markup are covered for both branches.
- No other pending fact or Phase 4 behavior becomes public.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-11-SUMMARY.md` when done.</output>
