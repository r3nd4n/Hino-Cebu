---
phase: 03-truck-discovery-local-support-routes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/discovery-routes.test.mjs
  - src/content/inquiry.ts
  - src/content/trucks.ts
  - src/components/shared/PageHero.tsx
  - src/components/shared/LocalContactCta.tsx
  - src/components/trucks/TruckCard.tsx
  - src/components/trucks/TruckSeriesPage.tsx
  - src/app/trucks/page.tsx
  - src/app/trucks/[slug]/page.tsx
autonomous: true
requirements: [DISC-01, DISC-02]
must_haves:
  truths:
    - "A visitor can scan four image-led truck cards with broad application guidance, a visible suitability/availability notice, and a page-level inquiry path (D-01, D-02, D-03)."
    - "Every truck card reaches a complete series route; 200 Series and Bus & PUV provide honest lightweight inquiry pages rather than dead ends (D-04)."
    - "The 300 and 500 Series pages lead with distinct business applications and then show exactly three curated, qualified highlights through one shared page structure (D-05, D-06, D-08)."
    - "Official product URLs and provenance remain typed maintainer metadata and never appear in rendered props, DOM, alt text, or visitor-facing links (D-07)."
    - "Every series route says Cebu availability and detailed requirements need local confirmation and offers both inquiry and verified call paths (D-09, D-13)."
  artifacts:
    - path: tests/discovery-routes.test.mjs
      provides: Wave 0 discovery and provenance regression contract.
    - path: src/content/trucks.ts
      provides: Four-range public view models plus private 300/500 provenance.
      contains: "getPublicTruckSeries"
    - path: src/app/trucks/page.tsx
      provides: Configurable four-card listing route.
    - path: src/app/trucks/[slug]/page.tsx
      provides: Finite static routes for all four configured slugs.
      exports: [generateStaticParams, default]
    - path: src/components/trucks/TruckSeriesPage.tsx
      provides: Shared rich/lightweight application-first detail template.
  key_links:
    - from: src/app/trucks/page.tsx
      to: src/content/trucks.ts
      via: map over the public truck-range projection.
      pattern: "publicTruckSeries|truckRanges"
    - from: src/app/trucks/[slug]/page.tsx
      to: src/content/trucks.ts
      via: finite static params and guarded public-series lookup.
      pattern: "generateStaticParams|getPublicTruckSeries"
    - from: src/components/trucks/TruckSeriesPage.tsx
      to: src/components/shared/LocalContactCta.tsx
      via: allowlisted series topic and configured phone action.
      pattern: "LocalContactCta"
    - from: src/content/trucks.ts
      to: src/components/trucks/TruckSeriesPage.tsx
      via: public projection that excludes private source metadata.
      pattern: "getPublicTruckSeries"
---

## Phase Goal

**As a** Cebu business visitor, **I want to** explore Hino truck ranges and local support routes, **so that** I can begin a credible local sales or service conversation.

<objective>
Deliver the complete truck-discovery journey from four application-guided cards to useful application-first series pages and contextual local inquiry/call actions.

Purpose: satisfy truck discovery without turning national catalog content into Cebu availability, fitment, or model claims.
Output: Wave 0 discovery tests, typed product/provenance contracts, reusable page/CTA components, `/trucks`, and all four finite series routes.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-RESEARCH.md
@.planning/phases/03-truck-discovery-local-support-routes/03-PATTERNS.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VALIDATION.md

<interfaces>
From src/content/trucks.ts: `TruckSeriesSlug = "200-series" | "300-series" | "500-series" | "bus-puv"`; existing `TruckRange` and `truckRanges` are the slug registry.
From src/content/assets.ts: `officialAssets.truck200`, `truck300`, `truck500`, and `truckBusPuv` provide local image src, alt, width, and height; source/provenance fields are not public component props.
From src/content/site.ts: `siteConfig.contact.phone.display` and `.href` are the verified call contract; `siteConfig.availabilityNotice` is configurable but the UI-SPEC exact listing/detail safeguards control this route copy.
From src/components/ui/Button.tsx: retain the existing button/link variants rather than adding a control system.
From Next.js 16 research: page `params` is asynchronous; finite dynamic routes use `generateStaticParams`, `dynamicParams = false`, and `notFound()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the failing truck-discovery route contract</name>
  <read_first>
    <file>tests/discovery-routes.test.mjs (create if absent)</file>
    <file>tests/homepage.test.mjs</file>
    <file>src/content/trucks.ts</file>
    <file>src/components/homepage/TruckRangeSection.tsx</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VALIDATION.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>tests/discovery-routes.test.mjs</files>
  <behavior>
    - The listing composes four configured single-link cards, static non-prescriptive application cues, the exact listing disclaimer, and a contextual inquiry/call panel.
    - The finite detail route generates exactly the four configured slugs, rejects unknown params, and renders the shared template.
    - 300/500 have distinct applications and exactly three qualified highlights; 200/Bus use the approved lightweight state with no invented highlights, models, or specifications.
    - Public route/component sources contain no Hino mother-site URLs or provenance identifiers and expose no Promotions surface.
  </behavior>
  <action>Create the Wave 0 `tests/discovery-routes.test.mjs` with Node test-runner source-contract assertions modeled on `tests/homepage.test.mjs`. Assert the exact four slug/href pairs, `generateStaticParams`, `dynamicParams = false`, `notFound`, listing disclaimer, single-link card anatomy, application-before-highlight structure, rich versus lightweight branching, exactly three highlight records for 300 and 500, contextual `inquiryHref` use, availability safeguards, and no `sourceUrl`, `source.url`, publisher/review fields, Hino mother-site URL, Promotions, price, financing, specification table, brochure, model-name section, filtering, comparison, or recommendation-engine copy in public route/component sources. This is the required failing end-to-end/content contract before implementation; do not weaken assertions to match the current missing routes.</action>
  <acceptance_criteria>
    <criterion>`tests/discovery-routes.test.mjs` exists and reads the planned route, component, and content files through parallel `readFile` calls.</criterion>
    <criterion>The test explicitly asserts D-01 through D-09 behavior and fails because Phase 3 discovery artifacts do not yet exist.</criterion>
    <criterion>The negative assertions distinguish private metadata in `src/content/trucks.ts` from forbidden references in public route/component sources.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/discovery-routes.test.mjs</automated>
    <manual>Confirm the command fails only on the intentionally absent Phase 3 discovery implementation, establishing RED.</manual>
  </verify>
  <done>A deterministic failing contract covers listing, all four detail routes, claim safety, internal-only provenance, conversion paths, and the no-promotions boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the configured listing and four finite series journeys</name>
  <read_first>
    <file>src/content/inquiry.ts (create if absent)</file>
    <file>tests/discovery-routes.test.mjs</file>
    <file>src/content/trucks.ts</file>
    <file>src/app/page.tsx</file>
    <file>src/components/homepage/HomepageSupportSections.tsx</file>
    <file>src/components/shared/PageHero.tsx (create if absent)</file>
    <file>src/components/shared/LocalContactCta.tsx (create if absent)</file>
    <file>src/components/trucks/TruckCard.tsx (create if absent)</file>
    <file>src/components/trucks/TruckSeriesPage.tsx (create if absent)</file>
    <file>src/app/trucks/page.tsx (create if absent)</file>
    <file>src/app/trucks/[slug]/page.tsx (create if absent)</file>
    <file>src/content/assets.ts</file>
    <file>src/content/site.ts</file>
    <file>src/components/homepage/TruckRangeSection.tsx</file>
    <file>src/components/homepage/FinalQuoteCta.tsx</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-RESEARCH.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>src/content/inquiry.ts, src/content/trucks.ts, src/components/shared/PageHero.tsx, src/components/shared/LocalContactCta.tsx, src/components/trucks/TruckCard.tsx, src/components/trucks/TruckSeriesPage.tsx, src/app/trucks/page.tsx, src/app/trucks/[slug]/page.tsx</files>
  <behavior>
    - Selecting any listing card opens an actionable series page; selecting Request Information carries only its typed topic to `/contact#inquiry`, and Call always uses configured phone data.
    - Application guidance uses `explore`, `ask`, `confirm`, and `tell us`; it never uses categorical `best`, `ideal`, `perfect`, `recommended`, or `fits` language.
    - 300/500 public content is a projection of typed sourced content; provenance remains available to maintainers but cannot cross the rendering boundary.
  </behavior>
  <action>Define `InquiryTopic`, `inquiryTopics`, `normalizeInquiryTopic(value)`, and `inquiryHref(topic)` in `src/content/inquiry.ts` for exactly `general`, `200-series`, `300-series`, `500-series`, `bus-puv`, `parts`, and `service`; keys and visitor labels must remain separate and unknown values normalize to `general`. Extend `src/content/trucks.ts` with immutable applications, image keys, rich/lightweight mode, exactly three theme-level 300/500 highlights, the exact UI-SPEC availability copy, and private `ContentSource` metadata (`publisher`, `url`, `reviewedOn`, `supports`). Use the official 300/500 URLs only there. Omit model names and all numerical specifications per the discretion resolution. Export public projection helpers whose returned type excludes every provenance field. Implement `PageHero`, `LocalContactCta`, `TruckCard`, and `TruckSeriesPage` using Server Components, local `next/image` fields only, a single link per navigable card, semantic headings, and existing visual primitives. Build `/trucks` per D-01â€“D-03 with four equal cards, exact page notice, and dark closing panel. Build `[slug]` with async params, `generateStaticParams`, `dynamicParams = false`, `notFound()`, the shared application-first structure, 300/500 distinct narratives, and the exact honest lightweight content for 200/Bus per D-04â€“D-09. Place a repeated availability safeguard immediately before the local CTA. Inquiry is primary and verified Call secondary on truck pages per D-13. Do not pass a whole asset or sourced-content record to public components; pass only display fields.</action>
  <acceptance_criteria>
    <criterion>`/trucks` renders all four configured cards in order with the exact listing disclaimer and no filter, matcher, comparison, pricing, finance, promotion, or availability badge.</criterion>
    <criterion>`/trucks/300-series` and `/trucks/500-series` share one component structure, lead with applications, and each render exactly three distinct qualified highlights.</criterion>
    <criterion>`/trucks/200-series` and `/trucks/bus-puv` render configured identity/image/application guidance plus the exact lightweight confirmation panel and both inquiry/call actions.</criterion>
    <criterion>Public props and JSX contain no official URL, publisher, review date, supported-fields list, asset provenance, model list, or technical table.</criterion>
    <criterion>Unknown truck slugs are excluded by static params and guarded by `notFound()`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/discovery-routes.test.mjs</automated>
    <manual>Review every visible application/highlight sentence for broad orientation, `select models`/`varies by model` qualifications where needed, and no implication of confirmed Cebu fit or stock.</manual>
  </verify>
  <done>All four truck journeys are reachable, configurable, promotion-free, source-governed, availability-safe, and converge on a local inquiry or verified call without visitor-facing mother-site links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| private typed provenance â†’ public Server Component view model | Official URLs and maintainer review metadata must be projected out before rendering. |
| configured national claims â†’ local visitor interpretation | National product content cannot imply Cebu stock, fitment, authorization, or model availability. |
| series topic â†’ contact URL | Only compile-time `InquiryTopic` values may construct inquiry links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Information Disclosure | `getPublicTruckSeries`, public route/component props | mitigate | Return an explicit public projection excluding `source`, `sourceUrl`, publisher, review, support, authorization, and replacement fields; regression-test public sources and rendered contracts. |
| T-03-02 | Tampering | product/application copy | mitigate | Keep all copy in typed immutable configuration, prohibit free-form query/path interpolation, and qualify model-dependent themes. |
| T-03-03 | Spoofing | Cebu availability/suitability implication | mitigate | Render the exact listing and per-series confirmation notices and omit model/spec/stock badges. |
| T-03-04 | Elevation of Privilege | Server/Client component boundary | mitigate | Keep discovery pages and content projections as Server Components and add no `use client`, provider call, secret import, or raw HTML rendering. |
| T-03-SC | Tampering | npm installs | accept | No package installation occurs; existing `next/image`, `next/link`, and Lucide primitives are used. |
</threat_model>

<verification>
<automated>node --test tests/discovery-routes.test.mjs</automated>
<automated>npm run lint</automated>
</verification>

<success_criteria>
- Visitors can move from four safe application-guided cards to four complete series routes.
- 300/500 provide useful sourced highlights without exposing provenance or implying Cebu availability.
- 200/Bus remain honest, actionable, and visually complete without invented product detail.
- Every truck route preserves inquiry and verified call actions and contains no promotion surface.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-01-SUMMARY.md` when done.</output>
