---
phase: 03-truck-discovery-local-support-routes
plan: "03"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tests/inquiry-demo.test.mjs
  - src/lib/inquiry-demo.ts
  - src/components/contact/InquiryForm.tsx
  - src/app/contact/page.tsx
autonomous: true
requirements: [DISC-03, DISC-04]
must_haves:
  truths:
    - "Truck, parts, service, and general CTAs converge on one `#inquiry` section on `/contact` with an allowlisted origin available for later attribution (D-10, D-11)."
    - "The visitor sees an editable `Inquiry about` select initialized from normalized context while the original normalized topic remains separately preserved (D-11 discretion resolution)."
    - "The form validates locally, prevents duplicate activation, and shows the approved local confirmation without claiming sent, received, provider delivery, a lead ID, or a follow-up promise (D-12)."
    - "Contact shows the verified phone, address, hours, address-search map, and plain awaiting-confirmation rows for unresolved email and verified directions; it creates no guessed or inactive links (D-18, D-19)."
    - "The form and configured click-to-call path are both immediately available (D-13)."
  artifacts:
    - path: tests/inquiry-demo.test.mjs
      provides: Wave 0 allowlist, validation, Contact, and truthful-state regression contract.
    - path: src/lib/inquiry-demo.ts
      provides: Pure local inquiry validation/result contract with no delivery side effect.
      exports: [validateInquiryDraft]
    - path: src/components/contact/InquiryForm.tsx
      provides: Narrow accessible client interaction island.
    - path: src/app/contact/page.tsx
      provides: Server-owned Contact facts, query normalization, map, and shared inquiry anchor.
  key_links:
    - from: src/app/contact/page.tsx
      to: src/content/inquiry.ts
      via: await `searchParams` then normalize untrusted `topic` before rendering.
      pattern: "normalizeInquiryTopic"
    - from: src/app/contact/page.tsx
      to: src/components/contact/InquiryForm.tsx
      via: safe `initialTopic` prop only.
      pattern: "initialTopic"
    - from: src/components/contact/InquiryForm.tsx
      to: src/lib/inquiry-demo.ts
      via: pure local validation before local status transition.
      pattern: "validateInquiryDraft"
    - from: src/app/contact/page.tsx
      to: src/content/site.ts
      via: verified/unresolved contact rendering and address-derived map query.
      pattern: "siteConfig"
---

## Phase Goal

**As a** Cebu business visitor, **I want to** carry my truck or support interest into one accessible local inquiry page, **so that** I can validate my details and choose the verified contact path.

<objective>
Deliver the shared Contact inquiry destination with safe topic normalization, truthful local demo behavior, verified Cebu facts, and explicit unresolved statuses.

Purpose: complete the Phase 3 conversion journey while keeping production submission, server validation, attribution delivery, Sheets, Resend, rate limiting, and provider errors inside Phase 4.
Output: Wave 0 inquiry tests, pure local validator, narrow client form, and server-owned `/contact` route.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-RESEARCH.md
@.planning/phases/03-truck-discovery-local-support-routes/03-PATTERNS.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VALIDATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-01-SUMMARY.md

<interfaces>
From 03-01 `src/content/inquiry.ts`: `InquiryTopic`, `inquiryTopics`, `normalizeInquiryTopic(value): InquiryTopic`, and `inquiryHref(topic)` are the sole topic/url registry.
From src/content/site.ts: approved phone/address/hours plus unresolved `email` and `directionsUrl`; null configured values never become anchors.
From src/lib/quote-demo.ts: mirror the pure discriminated `validateQuoteDraft` pattern and Philippine mobile/email checking, but use the Phase 3 inquiry fields/result copy.
From src/components/homepage/HomepageQuoteExperience.tsx: reuse the accessible idle/loading/success/failure state shape, persistent labels, native controls, inline errors, and no-network local timer boundary.
From Next.js 16 research: Contact `searchParams` is a Promise and must be awaited in the Server Component before normalizing `topic`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the failing allowlist, Contact, and demo-state contract</name>
  <read_first>
    <file>tests/inquiry-demo.test.mjs (create if absent)</file>
    <file>tests/quote-demo.test.mjs</file>
    <file>src/lib/quote-demo.ts</file>
    <file>src/content/inquiry.ts</file>
    <file>src/content/site.ts</file>
    <file>src/components/homepage/HomepageQuoteExperience.tsx</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VALIDATION.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>tests/inquiry-demo.test.mjs</files>
  <behavior>
    - Every allowed topic survives normalization and arbitrary strings, arrays, objects, empty values, and prototype-like keys fall back to `general` without reflection.
    - Ordered local validation rejects missing required fields, malformed Philippine mobile/email values, and unchecked consent, then returns only the approved local confirmation for a valid draft.
    - Contact awaits and normalizes search params server-side, initializes an editable visible topic, preserves normalized origin separately, and exposes no provider/API/secret behavior.
    - Unresolved email/directions are exact plain status rows; the verified phone is live, and the address-search map is never labeled as the verified branch listing/directions URL.
  </behavior>
  <action>Create `tests/inquiry-demo.test.mjs` using the existing `--experimental-strip-types` subprocess pattern to import both `normalizeInquiryTopic` and `validateInquiryDraft`. Assert every allowed key, hostile/unexpected input fallback, deterministic error fields, valid local-only confirmation, and absence of `sent`, `received`, follow-up promise, lead ID, persistence, or delivery wording. Add source-contract assertions for async `searchParams`, normalization before `InquiryForm`, the visible editable first-field topic select, separate immutable `originTopic`, exact form field order, accessible errors/live loading/local confirmation, configured contact imports, exact unresolved rows, address-derived map label/title, no raw query reflection, no `dangerouslySetInnerHTML`, no `fetch`, Server Action, provider, credential, storage, thank-you redirect, guessed email/directions anchor, raw error output, mother-site link, or Promotions surface. This task establishes RED and must fail only because the Contact/inquiry implementation is absent.</action>
  <acceptance_criteria>
    <criterion>`tests/inquiry-demo.test.mjs` covers all seven topic keys plus non-string and hostile fallback cases.</criterion>
    <criterion>The form contract asserts the UI-SPEC order: topic, name, mobile, email, company, message, consent, submit.</criterion>
    <criterion>The exact confirmation, immediate-call, safe-error, email-status, and directions-status copy is asserted.</criterion>
    <criterion>The test blocks any Phase 4 server/provider behavior or claim of delivery.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/inquiry-demo.test.mjs</automated>
    <manual>Confirm the RED failure is caused by missing inquiry-demo, form, and Contact artifacts rather than a broken test harness.</manual>
  </verify>
  <done>A failing Wave 0 contract defines untrusted-topic handling, local validation, truthful state semantics, Contact fact rendering, and the Phase 4 boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared Contact inquiry experience</name>
  <read_first>
    <file>src/lib/inquiry-demo.ts (create if absent)</file>
    <file>src/components/contact/InquiryForm.tsx (create if absent)</file>
    <file>src/app/contact/page.tsx (create if absent)</file>
    <file>src/app/page.tsx</file>
    <file>tests/inquiry-demo.test.mjs</file>
    <file>src/content/inquiry.ts</file>
    <file>src/content/site.ts</file>
    <file>src/lib/quote-demo.ts</file>
    <file>src/components/homepage/HomepageQuoteExperience.tsx</file>
    <file>src/components/homepage/HomepageSupportSections.tsx</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-RESEARCH.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>src/lib/inquiry-demo.ts, src/components/contact/InquiryForm.tsx, src/app/contact/page.tsx</files>
  <behavior>
    - The untrusted URL topic is normalized on the server; the client sees only an `InquiryTopic` and never arbitrary query text.
    - The visitor may edit `inquiryTopic`, while `originTopic` remains the initial normalized value for the Phase 4 handoff.
    - Valid local submit moves idle â†’ loading â†’ local confirmation without network I/O; invalid submit exposes field errors and prevents confirmation.
    - Contact presents an immediate verified call alternative, then exact configured/unresolved facts, an address-search map, and a closing call cue after the map.
  </behavior>
  <action>Create `InquiryDraft` with `originTopic`, editable `inquiryTopic`, `name`, `mobile`, `email`, `company`, `message`, and `consent`; define the discriminated `InquiryValidationResult` and `validateInquiryDraft` in `src/lib/inquiry-demo.ts`. Normalize/trim strings for validation only, require topic/name/mobile/consent, validate Philippine mobile and non-empty email only when supplied, and follow the UI-SPEC/shared schema for optional company/message; return the exact local confirmation contract without mutating, persisting, sanitizing for a provider, or sending data. Implement only `src/components/contact/InquiryForm.tsx` as `use client`. Initialize both topics from `initialTopic`, let only `inquiryTopic` change, render it first as a native labeled select, then the exact field order and accessible native controls. Use persistent labels, correct autocomplete/inputMode/type, `aria-invalid`, `aria-describedby`, a polite live region, disabled/loading duplicate prevention, focus to first invalid field or confirmation heading, exact confirmation/immediate-call/safe-failure copy, and the existing 300ms local transition with an explicit Phase 4 replacement comment. Do not add `fetch`, server action, persistence, provider import, lead ID, or thank-you route. Implement async `/contact` as a Server Component: await `searchParams`, pass `normalizeInquiryTopic(query.topic)` to the form, own the only `section#inquiry`, place Call prominently beside the form heading, source verified phone/address/hours from `siteConfig`, render `Email: awaiting confirmation` and `Verified directions link: awaiting confirmation` as plain text, retain the established encoded address-search map in a meaningful 16:10 frame with an accessible address-search alternative, and render the UI-SPEC-required closing call cue immediately after the map using the configured phone link. Never label the map as a verified branch listing or directions URL. Keep maintainer metadata and raw query values out of Client props per D-10â€“D-13 and D-18â€“D-19.</action>
  <acceptance_criteria>
    <criterion>Arbitrary `?topic=` values never render and always initialize both topic fields as `general`; each allowed CTA topic initializes both to its stable key.</criterion>
    <criterion>The topic select is first, visible, labeled, editable, and cannot mutate the preserved `originTopic`.</criterion>
    <criterion>Valid submit shows exactly `Thank you for your interest in Hino Cebu.` and `For immediate assistance, call (032) 346 3322.` after a local loading state, with no delivery claim or redirect.</criterion>
    <criterion>Invalid fields are textually identified and programmatically associated; repeat activation while loading is blocked.</criterion>
    <criterion>Only verified phone is an operational contact link; unresolved email/directions are exact non-interactive text and the map is explicitly an address search.</criterion>
    <criterion>The Contact route ends with a visible configured click-to-call cue immediately after the map.</criterion>
    <criterion>No Client Component receives source/provenance, raw `searchParams`, environment data, or server-only imports.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/inquiry-demo.test.mjs</automated>
    <manual>Exercise `?topic=300-series`, `?topic=parts`, and an arbitrary HTML-like topic; confirm safe editable labels, preserved origin behavior, errors, loading, confirmation focus, and no reflected query content.</manual>
  </verify>
  <done>The shared Contact route safely carries allowlisted context, validates accessibly and locally, renders truthful status, and exposes only configured operational contact facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL `searchParams.topic` â†’ server-rendered Contact props | Query input is untrusted and must be allowlisted before display or client serialization. |
| visitor form input â†’ client validation/status | Phase 3 validation is UX-only and must not be mistaken for secure submission. |
| server-owned site configuration â†’ Client Component | Only normalized topic and public display data may cross; no secret/provider/provenance module enters the client bundle. |
| unresolved configuration â†’ operational link UI | Null email/directions cannot become guessed, empty, disabled, or fallback anchors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-08 | Tampering | `normalizeInquiryTopic` / Contact query | mitigate | Membership-check exact typed keys, accept unknown types, fall back to `general`, and render plain React text only. |
| T-03-09 | Repudiation | local demo confirmation | mitigate | Use exact non-delivery wording, preserve Call, and prohibit `sent`, `received`, lead ID, redirect, or follow-up promise. |
| T-03-10 | Information Disclosure | `InquiryForm` client boundary | mitigate | Pass only normalized `InquiryTopic`; add no env/provider/server import, raw query, provenance object, raw exception, persistence, or network call. |
| T-03-11 | Spoofing | unresolved email/directions | mitigate | Render exact awaiting-confirmation plain text and label address-derived maps/search links accurately. |
| T-03-12 | Denial of Service | local submit control | mitigate | Ignore repeated submit while loading and disable the submit control; no backend resource is invoked in Phase 3. |
| T-03-SC | Tampering | npm installs | accept | No package installation occurs in this plan. |
</threat_model>

<verification>
<automated>node --test tests/inquiry-demo.test.mjs</automated>
<automated>npm run lint</automated>
</verification>

<success_criteria>
- Every Phase 3 CTA topic is safely normalized, visibly editable, and separately preserved for the Phase 4 handoff.
- The Contact form is accessible and truthful, and performs no delivery or persistence.
- Contact publishes only configured facts, explicit unresolved statuses, and accurately labeled address-search map behavior.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-03-SUMMARY.md` when done.</output>
