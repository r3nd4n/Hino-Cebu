---
phase: 03-truck-discovery-local-support-routes
plan: "06"
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - src/app/globals.css
  - src/components/shared/PageHero.tsx
  - src/components/shared/LocalContactCta.tsx
  - src/components/trucks/TruckCard.tsx
  - src/components/trucks/TruckSeriesPage.tsx
  - tests/discovery-routes.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-01, DISC-02, DISC-04]
must_haves:
  truths:
    - "Dark PageHero and LocalContactCta secondary actions have readable light text and borders in compiled output."
    - "No plain `<style>` block in shared or truck-discovery components contains styled-jsx-only `:global()` syntax."
    - "Truck media and icons retain their intended containment and color after selector repair."
  artifacts:
    - path: src/app/globals.css
      provides: Valid global descendant rules for shared/discovery component output.
    - path: tests/discovery-routes.test.mjs
      provides: Selector-audit and compiled-artifact prerequisites for discovery routes.
  key_links:
    - from: src/app/globals.css
      to: src/components/shared/PageHero.tsx
      via: Valid class descendants in the global stylesheet.
      pattern: "page-hero__actions.*button--secondary"
---

## Phase Goal

**As a** visitor exploring Hino truck routes, **I want to** see every conversion action clearly, **so that** I can continue to inquiry without an unreadable control.

<objective>
Repair invalid selector syntax on shared and discovery surfaces and protect the compiled contract.

Purpose: close CR-01 for the highest-reuse components before downstream fact work consumes them.
Output: valid selectors, preserved visual behavior, and executable selector/build assertions.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md
@.planning/phases/03-truck-discovery-local-support-routes/03-04-SUMMARY.md

<interfaces>
`PageHero`, `LocalContactCta`, `TruckCard`, and `TruckSeriesPage` use ordinary `<style>` elements, where `:global()` is invalid browser syntax. Homepage components using `<style jsx>` are a distinct valid case and must only be audited, not mechanically rewritten.
The base `.button--secondary` is dark ink; dark surfaces therefore require an explicit paper-colored foreground and border in valid compiled CSS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the plain-style selector audit</name>
  <read_first>
    <file>src/components/shared/PageHero.tsx</file>
    <file>src/components/shared/LocalContactCta.tsx</file>
    <file>src/components/trucks/TruckCard.tsx</file>
    <file>src/components/trucks/TruckSeriesPage.tsx</file>
    <file>src/app/globals.css</file>
    <file>tests/discovery-routes.test.mjs</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md</file>
  </read_first>
  <files>tests/discovery-routes.test.mjs</files>
  <behavior>
    - The audit fails if any ordinary `<style>` body contains `:global(`.
    - The audit distinguishes valid `<style jsx>` occurrences and reports every source occurrence rather than assuming all are invalid.
    - The contract requires valid dark-surface secondary-action and media/icon descendants.
  </behavior>
  <action>Add an executable repository audit that enumerates every `:global(` occurrence under `src`, classifies it by owning `<style>` versus `<style jsx>` block, and fails on any plain-style occurrence. Make failure output name each file and selector. Assert the valid replacement contract for light-on-dark secondary buttons plus image/icon descendants; avoid comment-count or unfiltered regex gates that can self-satisfy.</action>
  <acceptance_criteria>
    <criterion>The RED test reports all plain-style occurrences in the four owned components before implementation.</criterion>
    <criterion>Styled-jsx occurrences are explicitly classified and not falsely rejected.</criterion>
    <criterion>The test cannot pass because the forbidden token appears only in comments or test prose.</criterion>
  </acceptance_criteria>
  <verify><automated>node --test tests/discovery-routes.test.mjs</automated></verify>
  <done>A durable audit precisely distinguishes invalid plain-style selectors from valid styled-jsx syntax.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair shared and discovery selectors</name>
  <read_first>
    <file>src/app/globals.css</file>
    <file>src/components/shared/PageHero.tsx</file>
    <file>src/components/shared/LocalContactCta.tsx</file>
    <file>src/components/trucks/TruckCard.tsx</file>
    <file>src/components/trucks/TruckSeriesPage.tsx</file>
    <file>tests/discovery-routes.test.mjs</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>src/app/globals.css, src/components/shared/PageHero.tsx, src/components/shared/LocalContactCta.tsx, src/components/trucks/TruckCard.tsx, src/components/trucks/TruckSeriesPage.tsx</files>
  <behavior>
    - Dark shared surfaces compile a valid light-text/light-border secondary CTA rule.
    - Truck images remain contained and route icons retain the red token.
    - The selector audit returns zero invalid plain-style occurrences in owned files.
  </behavior>
  <action>Move descendant rules that target imported components/elements into `globals.css`, or express them as valid ordinary CSS without `:global()`. Preserve CSS specificity and the UI-SPEC tokens; do not add inline color props, duplicate card systems, or page-specific brand colors. Ensure PageHero and LocalContactCta secondary actions explicitly compute from `--color-paper` on dark surfaces, with visible hover and focus inherited from the established button contract. Keep contained image dimensions/object-fit and red icon behavior. Run a build, then confirm generated server/static artifacts contain no raw `:global(` from these components.</action>
  <acceptance_criteria>
    <criterion>`rg -n ':global\\('` reports no occurrence inside ordinary style blocks in all owned files.</criterion>
    <criterion>Valid selectors for `.page-hero__actions .button--secondary` and `.local-contact-cta__actions .button--secondary` set paper foreground and border.</criterion>
    <criterion>Production artifacts contain neither raw owned `:global(` selectors nor missing dark-CTA override selectors.</criterion>
    <criterion>`node --test tests/discovery-routes.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build` exits 0.</criterion>
  </acceptance_criteria>
  <verify><automated>node --test tests/discovery-routes.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated></verify>
  <done>Shared and truck-discovery styles compile into valid, readable action and media rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| component style source → browser CSS parser | Framework-specific syntax must not leak into ordinary browser CSS. |
| global button defaults → dark conversion surfaces | Inherited defaults must preserve readable action contrast. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-20 | Denial of Service | conversion actions | mitigate | Enforce valid selectors and readable computed foreground/border on dark panels. |
| T-03-21 | Tampering | selector audit | mitigate | Enumerate all occurrences and classify by style-block type; ignore comments in zero gates. |
| T-03-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>

<verification>
<automated>node --test tests/discovery-routes.test.mjs</automated>
<automated>npm run lint</automated>
<automated>npm run build</automated>
</verification>

<success_criteria>
- Every shared/discovery plain style uses browser-valid selectors.
- Dark conversion actions are explicitly light-on-dark in compiled output.
- The audit covers every repository occurrence without breaking valid styled-jsx usage.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-06-SUMMARY.md` when done.</output>
