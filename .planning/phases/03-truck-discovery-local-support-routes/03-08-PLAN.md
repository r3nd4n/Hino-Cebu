---
phase: 03-truck-discovery-local-support-routes
plan: "08"
type: execute
wave: 5
depends_on: [03-05]
files_modified:
  - src/lib/inquiry-demo.ts
  - src/components/contact/InquiryForm.tsx
  - src/components/homepage/HomepageQuoteExperience.tsx
  - src/components/homepage/HomepageSupportSections.tsx
  - tests/inquiry-demo.test.mjs
  - tests/homepage-interaction.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-03, DISC-04]
must_haves:
  truths:
    - "Inquiry local validation/loading/success behavior is executable and never claims network delivery."
    - "Duplicate activation is rejected and no unreachable send-failure branch remains in the Phase 3 InquiryForm."
    - "Homepage assistance/location surfaces obey the same central approval projection and preserve inquiry paths."
  artifacts:
    - path: src/lib/inquiry-demo.ts
      provides: Pure executable local-only transition contract.
    - path: src/components/contact/InquiryForm.tsx
      provides: Truthful local-only form UI without a fictitious failure state.
    - path: tests/inquiry-demo.test.mjs
      provides: Behavior tests for validation, loading, duplicate prevention, and local confirmation.
  key_links:
    - from: src/components/contact/InquiryForm.tsx
      to: src/lib/inquiry-demo.ts
      via: Pure transition result consumed before scheduling local confirmation.
      pattern: "transition|validateInquiryDraft"
---

## Phase Goal

**As a** visitor submitting interest locally, **I want to** understand exactly what the Phase 3 form did, **so that** I am not told an inquiry was sent when no network operation occurred.

<objective>
Make the inquiry demo state machine truthful and executable, and remove remaining hard-coded/unapproved homepage contact facts.

Purpose: close WR-01, WR-02, and the form portion of WR-03 without introducing Phase 4 delivery behavior.
Output: pure local transition behavior, accurate UI states/copy, approval-aware homepage fallbacks, and behavior tests.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md
@.planning/phases/03-truck-discovery-local-support-routes/03-05-SUMMARY.md

<interfaces>
`validateInquiryDraft` is the pure Phase 3 validator. Extend its executable contract for local transition decisions instead of adding a provider adapter.
Per D-12, Phase 3 may validate and show a polished local confirmation, but it must not claim send, receipt, persistence, provider activity, or follow-up.
Use the 03-05 approved/unresolved contact projection for all phone/address/hours/map rendering.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the unreachable failure branch with a tested local transition</name>
  <read_first>
    <file>src/lib/inquiry-demo.ts</file>
    <file>src/components/contact/InquiryForm.tsx</file>
    <file>tests/inquiry-demo.test.mjs</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md</file>
  </read_first>
  <files>src/lib/inquiry-demo.ts, src/components/contact/InquiryForm.tsx, tests/inquiry-demo.test.mjs</files>
  <behavior>
    - Invalid draft returns ordered field errors and does not enter loading.
    - A valid idle draft enters one local pending transition; a second activation while pending is ignored.
    - Completion yields a local confirmation that claims no send, receipt, persistence, provider, or follow-up.
  </behavior>
  <action>Expose a small pure transition decision from `inquiry-demo.ts` covering invalid, accepted-local, and duplicate-pending outcomes, and consume it in `InquiryForm`. Keep validation order, first-invalid focus, editable inquiry topic, immutable normalized origin, disabled/loading UI, and confirmation focus/live announcement. Remove `failure` from `InquiryStatus`, remove the ineffective `try/catch`, and delete all `couldn't send` copy. Keep the timer only as presentation of the local transition and document Phase 4 replacement at the boundary. Build any approved phone assistance copy from the central projection; when unresolved, omit call claims and point to the general inquiry/contact context instead. Do not call `fetch`, add an action/endpoint/provider, or imply delivery.</action>
  <acceptance_criteria>
    <criterion>Executable tests invoke the transition contract for invalid, valid, duplicate-pending, and completed local outcomes.</criterion>
    <criterion>`InquiryForm` has no failure status, catch branch, or copy containing `send/sent/received/follow up`.</criterion>
    <criterion>Success remains `Thank you for your interest in Hino Cebu.` and any phone action appears only when approved.</criterion>
    <criterion>`node --test tests/inquiry-demo.test.mjs` exits 0.</criterion>
  </acceptance_criteria>
  <verify><automated>node --test tests/inquiry-demo.test.mjs</automated></verify>
  <done>The Phase 3 form states are behaviorally tested, reachable, and truthful about their local-only boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove hard-coded and unapproved facts from homepage support states</name>
  <read_first>
    <file>src/content/site.ts</file>
    <file>src/components/homepage/HomepageQuoteExperience.tsx</file>
    <file>src/components/homepage/HomepageSupportSections.tsx</file>
    <file>tests/homepage-interaction.test.mjs</file>
    <file>tests/inquiry-demo.test.mjs</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>src/components/homepage/HomepageQuoteExperience.tsx, src/components/homepage/HomepageSupportSections.tsx, tests/homepage-interaction.test.mjs, tests/inquiry-demo.test.mjs</files>
  <behavior>
    - Default homepage output contains no candidate phone/address/hours/map and no hard-coded phone-bearing copy.
    - Quote demo confirmation remains truthful and provides a safe contact/inquiry next step when phone is unresolved.
    - Approved fact fixtures reveal only the corresponding operational surface.
  </behavior>
  <action>Apply the central projection to homepage quote confirmation and visit/support sections because these shared public routes also publish the pending facts identified by the verifier. Replace hard-coded phone copy with approved configured display text; if unresolved, preserve the quote/contact inquiry path without a Call link. Do not construct map/search URLs unless the required address/map facts are approved. Replace premature visit/location treatment with truthful awaiting-confirmation messaging while keeping Parts & Service and inquiry navigation. Keep Phase 2's no-promotions, form fields, local validation, and success contract; do not broaden this task into Phase 4 lead behavior.</action>
  <acceptance_criteria>
    <criterion>`rg -n '\\(032\\) 346 3322|couldn.t send' src/components/contact src/components/homepage src/app/contact` returns zero production-code matches.</criterion>
    <criterion>Default homepage output exposes no unresolved candidate value or third-party map request and retains a useful contact/inquiry route.</criterion>
    <criterion>Homepage form fields, local success, no-promotions, and business-use interactions remain intact.</criterion>
    <criterion>`node --test tests/homepage-interaction.test.mjs tests/inquiry-demo.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build` exits 0.</criterion>
  </acceptance_criteria>
  <verify><automated>node --test tests/homepage-interaction.test.mjs tests/inquiry-demo.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated></verify>
  <done>All local-only form/homepage assistance states are truthful, configurable, and safe while dealer facts remain unresolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser form input → local state machine | Input is untrusted and Phase 3 validation is UX-only, not server security. |
| local timer → visitor confirmation | A presentation transition must not be represented as network delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-25 | Spoofing | inquiry confirmation | mitigate | Use local-interest acknowledgement only; prohibit delivery/provider claims. |
| T-03-26 | Denial of Service | duplicate form activation | mitigate | Pure transition rejects activation while pending and button remains disabled. |
| T-03-27 | Tampering | inquiry topic/input | mitigate | Retain allowlist normalization and deterministic local validation; Phase 4 owns server validation. |
| T-03-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>

<verification>
<automated>node --test tests/inquiry-demo.test.mjs tests/homepage-interaction.test.mjs</automated>
<automated>npm run lint</automated>
<automated>npm run build</automated>
</verification>

<success_criteria>
- Inquiry behavior tests execute state decisions instead of matching dormant copy.
- No Phase 3 UI claims to send an inquiry or exposes a fictitious failure.
- Homepage and inquiry assistance honor the same local-fact approval boundary.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-08-SUMMARY.md` when done.</output>
