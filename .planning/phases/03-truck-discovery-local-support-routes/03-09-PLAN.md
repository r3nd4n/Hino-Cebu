---
phase: 03-truck-discovery-local-support-routes
plan: "09"
type: execute
wave: 6
depends_on: [03-06, 03-07, 03-08]
files_modified:
  - src/app/globals.css
  - src/app/parts-service/page.tsx
  - src/app/contact/page.tsx
  - src/app/about/page.tsx
  - tests/phase3-runtime-contracts.test.mjs
  - tests/discovery-routes.test.mjs
  - tests/support-routes.test.mjs
  - tests/inquiry-demo.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04]
must_haves:
  truths:
    - "Every remaining plain-style `:global()` selector is removed, while valid styled-jsx occurrences are accounted for."
    - "A production server/build artifact test proves unresolved facts, raw selectors, public provenance, promotions, and false send states are absent."
    - "Generated route output retains inquiry paths and valid dark-surface CTA styling across all eight Phase 3 routes."
  artifacts:
    - path: tests/phase3-runtime-contracts.test.mjs
      provides: Production-build/server-output contract for the complete Phase 3 route set.
    - path: src/app/globals.css
      provides: Valid support/contact/about descendant and dark-action rules.
  key_links:
    - from: tests/phase3-runtime-contracts.test.mjs
      to: .next production server
      via: Start built application, fetch route HTML/CSS, and assert runtime output.
      pattern: "next start|fetch"
---

## Phase Goal

**As a** Cebu business visitor, **I want to** receive the same truthful, readable conversion experience from production output, **so that** green source checks reflect what the browser actually receives.

<objective>
Finish the selector sweep and replace source-regex confidence with a durable production runtime contract.

Purpose: close the final CSS and WR-03 test-design gaps before manual acceptance.
Output: valid support-route CSS, runtime/build-artifact tests, and complete automated gates.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VALIDATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md
@.planning/phases/03-truck-discovery-local-support-routes/03-06-SUMMARY.md
@.planning/phases/03-truck-discovery-local-support-routes/03-07-SUMMARY.md
@.planning/phases/03-truck-discovery-local-support-routes/03-08-SUMMARY.md

<interfaces>
The runtime test may use Node built-ins only: spawn the already-built Next production server on an isolated port, poll readiness, fetch routes/assets, and terminate the child in teardown. Do not add a browser/test package.
The eight routes are `/trucks`, four finite truck slugs, `/parts-service`, `/contact`, and `/about`.
Compiled-style visual contrast still requires the manual matrix in 03-10; this plan proves selector validity and required declarations, not pixel perception.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair remaining support, Contact, and About plain styles</name>
  <read_first>
    <file>src/app/globals.css</file>
    <file>src/app/parts-service/page.tsx</file>
    <file>src/app/contact/page.tsx</file>
    <file>src/app/about/page.tsx</file>
    <file>tests/discovery-routes.test.mjs</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md</file>
  </read_first>
  <files>src/app/globals.css, src/app/parts-service/page.tsx, src/app/contact/page.tsx, src/app/about/page.tsx, tests/discovery-routes.test.mjs</files>
  <behavior>
    - Plain styles in all three route files contain no `:global(`.
    - Form controls, icons, buttons, pending-status treatments, map surfaces, and responsive widths retain their UI-SPEC behavior.
    - Repository audit enumerates every remaining occurrence and accepts only occurrences enclosed by `<style jsx>`.
  </behavior>
  <action>Use the audit established in 03-06 to repair every invalid `:global()` selector in ordinary styles for Parts & Service, Contact, and About. Move cross-component/native descendant rules to `globals.css` with route-scoped selectors or use valid plain descendants; preserve 44px targets, control/error/success styles, 16:10 map contract when approved, icon colors, grid breakpoints, reduced motion, and dark CTA contrast. Run the full repository occurrence audit after edits and document each valid remaining `<style jsx>` occurrence through the test classification. Do not blindly remove styled-jsx syntax from Homepage components where it is valid.</action>
  <acceptance_criteria>
    <criterion>All source `:global(` occurrences are enumerated; zero belong to ordinary `<style>` blocks.</criterion>
    <criterion>Contact form native controls and status rows retain visible border, focus, error, and success rules.</criterion>
    <criterion>Support/About actions retain intended spacing and approved white/near-black/red tokens.</criterion>
    <criterion>`node --test tests/discovery-routes.test.mjs &amp;&amp; npm run lint` exits 0.</criterion>
  </acceptance_criteria>
  <verify><automated>node --test tests/discovery-routes.test.mjs &amp;&amp; npm run lint</automated></verify>
  <done>Every invalid plain-style occurrence is repaired without corrupting valid styled-jsx selectors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add production runtime and build-artifact regression gates</name>
  <read_first>
    <file>package.json</file>
    <file>tests/discovery-routes.test.mjs</file>
    <file>tests/support-routes.test.mjs</file>
    <file>tests/inquiry-demo.test.mjs</file>
    <file>src/content/site.ts</file>
    <file>src/lib/inquiry-demo.ts</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VALIDATION.md</file>
  </read_first>
  <files>tests/phase3-runtime-contracts.test.mjs, tests/discovery-routes.test.mjs, tests/support-routes.test.mjs, tests/inquiry-demo.test.mjs</files>
  <behavior>
    - Built production responses for all eight routes contain the shared shell, one main landmark, and an inquiry path.
    - Default responses contain no pending candidate phone/address/hours, `tel:`/map action, public mother-site URL, Promotions surface, or false send/failure copy.
    - Fetched HTML/CSS contains no raw `:global(` and includes valid dark secondary CTA declarations.
  </behavior>
  <action>Create `tests/phase3-runtime-contracts.test.mjs` using Node built-ins. It must require a fresh `.next` build, start `next start` on an isolated port, wait with a bounded retry, fetch all eight routes and referenced CSS, assert response status/output contracts, and always terminate the child process. Inspect generated production output rather than implementation source for selector leakage, approval gating, no-promotions/public-provenance, and inquiry reachability. Retain pure executable tests for approval projection and inquiry state transitions in the existing suites; demote source regex to narrow provenance/static-boundary checks only. Run the complete gate in the exact order `npm run build &amp;&amp; npm test &amp;&amp; npm run lint`; `npm test` includes `tests/phase3-runtime-contracts.test.mjs` through the existing test glob and therefore must never run before the fresh build, because stale artifacts are invalid evidence.</action>
  <acceptance_criteria>
    <criterion>The runtime test fails against stale/missing build output with an actionable instruction and passes only after a fresh successful build.</criterion>
    <criterion>All eight production responses return success and satisfy unresolved-fact, inquiry, provenance, promotions, and false-delivery assertions.</criterion>
    <criterion>Fetched production HTML/CSS contains no raw `:global(` and contains valid light-on-dark secondary CTA declarations.</criterion>
    <criterion>`npm run build &amp;&amp; npm test &amp;&amp; npm run lint` exits 0, with the glob-included runtime-contract suite inspecting only the immediately preceding fresh `.next` output.</criterion>
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm test &amp;&amp; npm run lint</automated></verify>
  <done>The green gate now proves behavior and generated production output instead of relying on source-string presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/build pipeline → production HTML/CSS | Tests must validate generated output rather than assume source intent survives compilation. |
| test process → local production server | The harness must use a bounded isolated port and reliably terminate its child. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-28 | Tampering | runtime regression evidence | mitigate | Require a fresh build and assert fetched output from all eight routes. |
| T-03-29 | Denial of Service | spawned test server | mitigate | Bound readiness polling and kill the child in test teardown on pass/fail. |
| T-03-30 | Information Disclosure | production route HTML | mitigate | Assert pending candidates and private provenance are absent from responses. |
| T-03-SC | Tampering | package installs | accept | Node built-ins and installed Next are sufficient; no package installation occurs. |
</threat_model>

<verification>
<automated>npm run build &amp;&amp; npm test &amp;&amp; npm run lint</automated>
</verification>

<success_criteria>
- No invalid plain-style selector reaches source or production output.
- Runtime route tests protect approval gating, inquiry reachability, no promotions, and private provenance.
- All tests, lint, fresh build, and post-build runtime checks pass.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-09-SUMMARY.md` when done.</output>
