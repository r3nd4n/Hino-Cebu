---
phase: 03-truck-discovery-local-support-routes
plan: "05"
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - src/content/site.ts
  - src/components/layout/Header.tsx
  - src/components/layout/MobileMenu.tsx
  - src/components/layout/MobileActionBar.tsx
  - src/components/layout/Footer.tsx
  - tests/support-routes.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-03, DISC-04]
must_haves:
  truths:
    - "Unapproved phone, address/map, and hours never render as settled public facts or active operational links."
    - "The shared shell remains usable while every local fact is unresolved: navigation and a general inquiry route remain available."
    - "Changing one fact to approved in authoritative configuration reveals only that fact's approved public value."
  artifacts:
    - path: src/content/site.ts
      provides: Status-bearing local fact contracts and a public projection that excludes unresolved values.
    - path: src/components/layout/Footer.tsx
      provides: Status-driven shared-shell local information.
    - path: tests/support-routes.test.mjs
      provides: Executable approval-projection and shared-shell fallback coverage.
  key_links:
    - from: src/components/layout/Footer.tsx
      to: src/content/site.ts
      via: Public contact projection rather than raw configured values.
      pattern: "publicContact|approved"
    - from: src/components/layout/Header.tsx
      to: /contact#inquiry
      via: Inquiry fallback when phone is unresolved.
      pattern: "/contact#inquiry"
---

## Phase Goal

**As a** Cebu business visitor, **I want to** see only confirmed dealer facts while retaining an inquiry route, **so that** I can seek local support without being misdirected.

<objective>
Create the central approval boundary for local contact facts and apply it to the shared shell.

Purpose: close the root trust-status defect before route components consume the same facts.
Output: typed status-bearing facts, a safe public projection, shared-shell fallbacks, and executable projection tests.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-04-SUMMARY.md

<interfaces>
From src/content/site.ts: `VerificationStatus = "approved" | "unresolved" | "requires-verification"` and `ConfiguredValue<T> { value; status; launchNote? }` already govern email, directions, and legal identity. Extend this contract to phone, address/map input, and hours.
From src/components/layout/MobileMenu.tsx: `phone` is currently a required `{ display; href }` prop; make the action status-driven without accepting unresolved raw values.
From D-13 and D-18: inquiry and click-to-call coexist only when the phone is approved; unresolved facts must not create inactive or fabricated links.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the local-fact approval projection</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md</file>
    <file>src/content/site.ts</file>
    <file>tests/support-routes.test.mjs</file>
  </read_first>
  <files>src/content/site.ts, tests/support-routes.test.mjs</files>
  <behavior>
    - Default unresolved phone, address, and hours project an awaiting-confirmation state without their raw value or href.
    - An approved fact projects its value while unrelated unresolved facts remain hidden.
    - Directions/map cannot become active from an unresolved address or unresolved directions value.
  </behavior>
  <action>Per D-18 and D-19, wrap phone, address, and hours in the existing `ConfiguredValue` contract and default them to `requires-verification` because `.planning/STATE.md` is the current source of truth. Add a central, typed public projection/helper whose discriminated result makes approved data versus an awaiting-confirmation fallback explicit; unresolved branches must not carry the raw display value, `tel:` href, address-derived map query, or hours array. Keep actual candidate values configurable for later authorized approval, but do not update STATE.md or require external approval to complete this plan. Add executable tests that load the TypeScript module and exercise unresolved, mixed-status, and approved fixtures; do not count source strings as behavior.</action>
  <acceptance_criteria>
    <criterion>`siteConfig.contact.phone`, `siteConfig.contact.address`, and `siteConfig.hours` each have explicit status and launch-note semantics.</criterion>
    <criterion>The default public projection contains no phone display/href, address/map query input, or hours rows because all three remain pending in STATE.md.</criterion>
    <criterion>Approved fixture values become public independently, and an unresolved address can never activate a map path.</criterion>
    <criterion>`node --test tests/support-routes.test.mjs` exits 0.</criterion>
  </acceptance_criteria>
  <verify><automated>node --test tests/support-routes.test.mjs</automated></verify>
  <done>The authoritative config can safely hold candidate facts while its public projection reveals only explicitly approved values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply approval gating to every shared-shell contact surface</name>
  <read_first>
    <file>src/content/site.ts</file>
    <file>src/components/layout/Header.tsx</file>
    <file>src/components/layout/MobileMenu.tsx</file>
    <file>src/components/layout/MobileActionBar.tsx</file>
    <file>src/components/layout/Footer.tsx</file>
    <file>tests/support-routes.test.mjs</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>src/components/layout/Header.tsx, src/components/layout/MobileMenu.tsx, src/components/layout/MobileActionBar.tsx, src/components/layout/Footer.tsx, tests/support-routes.test.mjs</files>
  <behavior>
    - With default unresolved facts, header, mobile menu/action bar, and footer expose no `tel:` link or candidate phone/address/hours text.
    - Each surface retains navigation to `/contact#inquiry` and labels pending local facts truthfully.
    - With an approved phone projection, call actions use exactly its configured display and href.
  </behavior>
  <action>Consume only the central public projection from Task 1. Replace required raw-phone props with an approved-or-unresolved contract. When the phone is unresolved, omit Call/tel actions and use a clear `Contact / Inquire` path to `/contact#inquiry`; do not render a disabled anchor. In Footer, show concise awaiting-confirmation rows for unresolved phone, address, and hours without leaking candidate values. Preserve skip link, main navigation, menu Escape/focus-return behavior, exact homepage quote behavior, non-home general inquiry mapping, and D-10/D-13 conversion reachability. Add rendered/server-output or executable component-contract assertions where possible; narrow source-leakage assertions may supplement but not replace projection tests.</action>
  <acceptance_criteria>
    <criterion>No shared-shell component dereferences unresolved `.value`, phone `.href`, or address/hours candidates.</criterion>
    <criterion>Default rendered shell contains no `tel:+63323463322`, candidate address, or candidate hours, and still links to `/contact#inquiry`.</criterion>
    <criterion>Header/mobile menu/mobile action/footer retain their accessible labels and existing navigation/focus contracts.</criterion>
    <criterion>`node --test tests/support-routes.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build` exits 0.</criterion>
  </acceptance_criteria>
  <verify><automated>node --test tests/support-routes.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated></verify>
  <done>The global shell remains conversion-capable without publishing any unapproved local fact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| private configured candidates → public projection | Pending commercial facts must not cross into rendered output. |
| public projection → shared client shell | Client components receive only approved operational actions or explicit unresolved state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-17 | Spoofing | local dealer identity/contact | mitigate | Status-gate every fact and strip unresolved raw values in the public projection. |
| T-03-18 | Tampering | shell action props | mitigate | Pass a discriminated approved/unresolved value, never an unchecked phone object. |
| T-03-19 | Information Disclosure | public HTML/client props | mitigate | Test default build output contains no pending candidate phone, address, or hours. |
| T-03-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>

<verification>
<automated>node --test tests/support-routes.test.mjs</automated>
<automated>npm run lint</automated>
<automated>npm run build</automated>
</verification>

<success_criteria>
- Local fact approval is centralized and independently testable.
- Default unresolved configuration is safe and still offers a general inquiry path.
- No external approval or STATE.md edit is required to execute this plan.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-05-SUMMARY.md` when done.</output>
