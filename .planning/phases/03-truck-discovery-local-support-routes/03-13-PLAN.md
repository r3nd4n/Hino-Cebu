---
phase: 03-truck-discovery-local-support-routes
plan: "13"
type: execute
wave: 8
depends_on: [03-10]
files_modified:
  - src/components/layout/MobileMenu.tsx
  - src/components/layout/MobileActionBar.tsx
  - src/components/homepage/HomepageQuoteExperience.tsx
  - src/app/globals.css
  - tests/phase3-browser-interactions.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-04]
must_haves:
  truths:
    - "Opening the full-screen mobile menu moves focus into it, keeps Tab and Shift+Tab inside, and makes obscured page/footer/quick-action content inert."
    - "Escape closes the menu and restores focus to the trigger without leaving inert or scroll-lock state behind."
    - "When phone is unresolved, the only mobile-bar inquiry action spans the available width."
    - "An invalid homepage consent checkbox exposes aria-invalid=true and its associated textual error."
  artifacts:
    - path: src/components/layout/MobileMenu.tsx
      provides: Modal-style focus lifecycle, containment, inert background, Escape close, and restoration.
    - path: src/app/globals.css
      provides: Single-action mobile bar layout without weakening existing responsive rules.
    - path: tests/phase3-browser-interactions.test.mjs
      provides: Failing then passing rendered regressions for menu, mobile bar, and homepage consent.
  key_links:
    - from: src/components/layout/MobileMenu.tsx
      to: main, footer, and quick actions
      via: reversible native inert state while panel is open
      pattern: "\.inert\s*=\s*true"
    - from: MobileActionBar unresolved branch
      to: src/app/globals.css
      via: single-action modifier class
      pattern: "mobile-action-bar--single"
---

## Phase Goal

**As a** keyboard or mobile visitor, **I want to** use the site shell and validation feedback without focus escaping or controls collapsing, **so that** every primary route remains operable at the required widths.

<objective>
Close the mobile shell focus blocker and the two tightly coupled responsive/accessibility warnings with rendered regressions.

Purpose: make the full-screen menu behave as a contained modal navigation surface while preserving the established shell and conversion hierarchy.
Output: focus-contained MobileMenu, full-width single mobile action, homepage consent semantics, and a glob-included browser regression scaffold.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md
@.planning/phases/03-truck-discovery-local-support-routes/03-10-ACCEPTANCE.md
@.planning/phases/03-truck-discovery-local-support-routes/03-10-browser-audit.mjs

<interfaces>
From src/components/layout/MobileMenu.tsx:
- isOpen mounts `.mobile-menu__panel`; triggerRef points to the persistent toggle button.
- The current effect only locks body overflow, listens for Escape, and focuses the trigger during cleanup.

From src/app/layout.tsx:
- Body order is Header, route main, Footer, MobileActionBar.
- The menu lives inside Header; obscured siblings can be selected as `main`, `.site-footer`, and `.mobile-action-bar` without making the panel itself inert.

From src/components/layout/MobileActionBar.tsx:
- Phone is omitted unless approved, but the nav always receives the two-column class.

From src/components/homepage/HomepageQuoteExperience.tsx:
- Consent already has aria-describedby and an inline error, but lacks aria-invalid.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish rendered mobile-shell and consent regressions</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md</file>
    <file>src/components/layout/MobileMenu.tsx</file>
    <file>src/components/layout/MobileActionBar.tsx</file>
    <file>src/components/homepage/HomepageQuoteExperience.tsx</file>
    <file>src/app/globals.css</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-10-browser-audit.mjs</file>
  </read_first>
  <files>tests/phase3-browser-interactions.test.mjs</files>
  <behavior>
    - At 390px keyboard-opening the menu focuses its first navigation link; repeated Tab and Shift+Tab wrap within panel links/actions and never reach obscured main/footer/mobile-bar controls.
    - While open, main, footer, and mobile action bar are inert; Escape closes, removes inert/scroll lock, and restores trigger focus.
    - With unresolved phone, the visible mobile action occupies the full inner nav width.
    - Empty homepage quote submission makes consent `aria-invalid="true"`, associates the error text, and focuses the first invalid field according to existing behavior.
  </behavior>
  <action>Create the test-glob file as a rendered Chrome DevTools Protocol test using Node built-ins, the installed Chrome executable, and the service/startup conventions from `03-10-browser-audit.mjs` and `tests/phase3-runtime-contracts.test.mjs`; install no package. Resolve Chrome from a `CHROME_PATH` override first, then the installed Windows Chrome locations already used for Plan 03-10 acceptance, and fail with an actionable message if none exists. Require the immediately preceding fresh build, allocate isolated production and Chrome debugging ports, launch both with hidden windows, guarantee teardown on pass/failure, and express the four behaviors above as DOM/focus/computed-layout assertions. This task is RED: do not weaken assertions to fit current behavior and do not replace them with TSX source scans.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test tests/phase3-browser-interactions.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The new rendered test starts and tears down production Next and installed Chrome without manual services.</criterion>
    <criterion>It fails on current missing initial focus/containment/inert, half-width single action, and consent aria-invalid behavior.</criterion>
    <criterion>No new package or source-string substitute is introduced.</criterion>
  </acceptance_criteria>
  <done>A durable failing browser contract reproduces the menu blocker and both coupled warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contain menu focus and repair mobile conversion semantics</name>
  <read_first>
    <file>src/components/layout/MobileMenu.tsx</file>
    <file>src/components/layout/MobileActionBar.tsx</file>
    <file>src/components/homepage/HomepageQuoteExperience.tsx</file>
    <file>src/app/globals.css</file>
    <file>tests/phase3-browser-interactions.test.mjs</file>
  </read_first>
  <files>src/components/layout/MobileMenu.tsx, src/components/layout/MobileActionBar.tsx, src/components/homepage/HomepageQuoteExperience.tsx, src/app/globals.css</files>
  <behavior>
    - Menu open behaves as one contained keyboard scope with deterministic first/last wrapping.
    - Cleanup exactly restores prior inert and body overflow values and returns focus to the opener.
    - Single-action mobile bar and homepage invalid consent are correct in rendered output.
  </behavior>
  <action>Add a panel ref and one open-state effect in MobileMenu that records prior body overflow and prior inert state for `main`, `.site-footer`, and `.mobile-action-bar`; marks those obscured siblings inert; moves focus after mount to the first visible panel link/action; handles Escape; and traps Tab/Shift+Tab across the current panel focusables with first/last wrapping. Cleanup must remove listeners, restore every recorded state, restore overflow, and focus the captured trigger. Keep native links, route-aware destinations, close-on-link activation, and existing proportional UI-SPEC styling; do not add a package or make the whole body/header inert. In MobileActionBar, add a deterministic single-action modifier when phone is not approved and make that modifier one column/span the full available width in globals.css at mobile sizes. In HomepageQuoteExperience, add `aria-invalid={Boolean(errors.consent)}` to the consent checkbox while retaining aria-describedby and its visible error. This implements the UI-SPEC focus/restoration contract and keeps D-13 inquiry reachability.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test tests/phase3-browser-interactions.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    <criterion>First focus, forward/backward wrapping, background inertness, Escape, cleanup, and trigger restoration all pass in rendered Chrome.</criterion>
    <criterion>No obscured main/footer/mobile-action control becomes the active element while the menu is open.</criterion>
    <criterion>The unresolved single action spans the bar and retains `/contact#inquiry` off-home behavior.</criterion>
    <criterion>Homepage consent exposes both aria-invalid and the associated visible error when invalid.</criterion>
  </acceptance_criteria>
  <done>The mobile shell is focus-contained and both coupled responsive/accessibility warnings are closed with rendered evidence.</done>
</task>

</tasks>

<source_coverage>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Consistent usable shared conversion shell | 03-13 | COVERED | Menu, mobile action, and homepage error semantics are operable. |
| REQ | DISC-04 | Primary routes render correctly with consistent navigation/footer | 03-13 | COVERED | Full-screen mobile navigation is keyboard-contained. |
| CONTEXT | D-13 | Inquiry and verified call paths remain available | 03-13 | COVERED | Single unresolved inquiry action expands; approved call branch remains. |
| RESEARCH | - | Responsive shell at required widths and narrow client interactions | 03-13 | COVERED | Existing clients are repaired and tested at 390px. |
| UI-SPEC | - | Menu Escape/focus restoration, sticky bar, aria-invalid, 44px controls | 03-13 | COVERED | Rendered checks cover focus lifecycle and form semantics. |
| REVIEW | WR-01 | Half-empty single-action mobile bar | 03-13 | COVERED | One-column modifier closes warning. |
| REVIEW | WR-02 | Menu lacks focus containment/inert background | 03-13 | COVERED | Native inert plus Tab trap and restoration close blocker. |
| REVIEW | WR-03 | Homepage consent lacks invalid signal | 03-13 | COVERED | Checkbox now exposes aria-invalid. |
</source_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| modal navigation to obscured document | Keyboard and assistive technology must not operate covered route/footer controls. |
| browser interaction test to local processes | Test-owned Next and Chrome processes must use isolated ports and guaranteed teardown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-47 | Denial of Service | MobileMenu focus loop | mitigate | Compute visible focusables on each key event and wrap only at first/last boundaries. |
| T-03-48 | Elevation of Privilege | obscured document controls | mitigate | Apply native inert to main/footer/quick actions while the modal panel is open. |
| T-03-49 | Repudiation | browser regression lifecycle | mitigate | Assert exact focus/inert/layout states and always tear down isolated processes. |
| T-03-SC | Tampering | package installs | accept | No install occurs; Node built-ins and installed Chrome are used. |
</threat_model>

<verification>
<automated>npm run build &amp;&amp; node --test tests/phase3-browser-interactions.test.mjs &amp;&amp; npm run lint</automated>
</verification>

<success_criteria>
- Mobile menu focus enters, remains within, and restores correctly.
- Obscured content is inert only while the menu is open.
- Unresolved single-action mobile bar uses the available width.
- Homepage consent exposes consistent invalid semantics.
- Rendered browser checks run without new dependencies or manual service setup.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-13-SUMMARY.md` when done.</output>
