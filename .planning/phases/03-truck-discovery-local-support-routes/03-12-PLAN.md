---
phase: 03-truck-discovery-local-support-routes
plan: "12"
type: execute
wave: 8
depends_on: [03-10]
files_modified:
  - src/lib/inquiry-demo.ts
  - src/components/contact/InquiryForm.tsx
  - tests/inquiry-demo.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-03]
must_haves:
  truths:
    - "After local confirmation, Start another inquiry restores a clean editable form instead of navigating to the same fragment."
    - "Reset preserves the normalized initial origin and topic, clears visitor-entered fields/errors/status, and moves focus to the restored topic control."
    - "Approved phone remains an adjacent immediate-assistance option and never replaces the restart action."
  artifacts:
    - path: src/lib/inquiry-demo.ts
      provides: Pure reset transition and normalized initial-draft contract.
    - path: src/components/contact/InquiryForm.tsx
      provides: Real success-to-idle reset action with focus restoration.
    - path: tests/inquiry-demo.test.mjs
      provides: Reset transition, stale-state clearing, and component wiring regression.
  key_links:
    - from: src/components/contact/InquiryForm.tsx
      to: src/lib/inquiry-demo.ts
      via: transitionInquiry reset event
      pattern: "event: \"reset\""
    - from: success confirmation
      to: restored inquiry topic control
      via: state reset followed by requestAnimationFrame focus
      pattern: "querySelector.*inquiryTopic.*focus"
---

## Phase Goal

**As a** Cebu business visitor, **I want to** start another inquiry after local confirmation, **so that** I can continue a complete support conversation without reloading or losing the original context rules.

<objective>
Replace the dead same-fragment success link with an executable reset transition and deterministic focus restoration.

Purpose: close the incomplete inquiry-journey blocker while preserving D-10 through D-12 and the Phase 4 attribution boundary.
Output: a pure reset contract, reset-capable InquiryForm, and executable state coverage.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-REVIEW.md
@.planning/phases/03-truck-discovery-local-support-routes/03-08-SUMMARY.md

<interfaces>
From src/lib/inquiry-demo.ts:
- InquiryDraft stores immutable originTopic separately from editable inquiryTopic.
- InquiryStatus is `idle | loading | success`.
- transitionInquiry currently accepts activate and complete and returns discriminated outcomes.

From src/components/contact/InquiryForm.tsx:
- The initial draft seeds both originTopic and inquiryTopic from normalized initialTopic.
- Success currently unmounts the form and renders a dead `/contact#inquiry` anchor when phone is unresolved.
- formRef and confirmationRef already support focus management.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the success-to-idle reset transition</name>
  <read_first>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VERIFICATION.md</file>
    <file>src/lib/inquiry-demo.ts</file>
    <file>src/components/contact/InquiryForm.tsx</file>
    <file>tests/inquiry-demo.test.mjs</file>
  </read_first>
  <files>tests/inquiry-demo.test.mjs</files>
  <behavior>
    - Reset from success returns idle, no errors, and a new draft with originTopic and inquiryTopic both equal to the supplied normalized initial topic.
    - Reset clears name, mobile, email, company, message, and consent regardless of the completed draft values.
    - The success UI contains a button action for restart in both approved and unresolved phone branches and contains no same-fragment restart anchor.
  </behavior>
  <action>Add failing executable transition tests for a `reset` event and the complete returned state. Keep the existing activate/duplicate/complete tests unchanged. Add only a narrow static boundary check that the success restart is a button wired to the reset handler rather than an href; rendered focus and interaction behavior belongs to the browser contract in Plan 03-14. Per D-11, assert both originTopic and inquiryTopic use the normalized initial topic after reset, not the visitor's last edited topic.</action>
  <verify>
    <automated>node --test tests/inquiry-demo.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The new reset transition test fails before implementation.</criterion>
    <criterion>Existing invalid/loading/duplicate/local-confirmation tests remain present and continue to pass independently.</criterion>
    <criterion>The test forbids a restart href without broadly source-testing client behavior.</criterion>
  </acceptance_criteria>
  <done>A failing contract defines the exact clean draft, idle status, normalized context, and real restart control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore a clean focused form from local confirmation</name>
  <read_first>
    <file>src/lib/inquiry-demo.ts</file>
    <file>src/components/contact/InquiryForm.tsx</file>
    <file>tests/inquiry-demo.test.mjs</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-08-SUMMARY.md</file>
  </read_first>
  <files>src/lib/inquiry-demo.ts, src/components/contact/InquiryForm.tsx</files>
  <behavior>
    - Clicking Start another inquiry after success re-renders the form with clean values and idle controls.
    - The inquiry topic and origin reset to initialTopic even if the visitor edited the topic before the prior submission.
    - Focus moves to the restored `inquiryTopic` select after React commits the form.
  </behavior>
  <action>Add one reusable initial-draft/reset contract to `inquiry-demo.ts` and extend `transitionInquiry` with a success-only reset input/result that returns the clean draft and idle status. In InquiryForm, replace the restart anchor with `type="button"` and a reset handler that consumes this transition, applies returned draft, clears errors, sets idle, and on the next animation frame focuses the restored `[name="inquiryTopic"]` control through formRef. Always render Start another inquiry; if phone is approved, retain Call Hino Cebu as a separate adjacent action. Preserve the local-only 300 ms completion, duplicate guard, confirmation copy, and absence of fetch/provider/persistence/failure claims per D-10 through D-12. Do not mutate initialTopic or derive the reset topic from the edited draft.</action>
  <verify>
    <automated>node --test tests/inquiry-demo.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Reset yields a blank draft, empty errors, idle status, normalized originTopic, and normalized inquiryTopic.</criterion>
    <criterion>The restored topic select receives focus after the form remounts.</criterion>
    <criterion>Restart remains available whether phone is approved or awaiting confirmation.</criterion>
    <criterion>No network, server action, provider, persistence, delivery claim, or Phase 4 behavior is added.</criterion>
  </acceptance_criteria>
  <done>The post-confirmation journey has a real, focus-managed reset transition instead of a dead fragment link.</done>
</task>

</tasks>

<source_coverage>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Continue a local contact conversation | 03-12 | COVERED | Confirmation reconnects to a usable inquiry form. |
| REQ | DISC-03 | Contact page has a clear complete next action | 03-12 | COVERED | Restart is executable and focus-managed. |
| CONTEXT | D-10 | Shared inquiry remains on Contact | 03-12 | COVERED | Reset stays inside the single Contact form. |
| CONTEXT | D-11 | Preserve normalized originating context | 03-12 | COVERED | Reset reseeds origin/topic from normalized initialTopic. |
| CONTEXT | D-12 | Local validation/demo success only | 03-12 | COVERED | No provider boundary is introduced. |
| CONTEXT | D-13 | Inquiry and approved call remain available | 03-12 | COVERED | Restart always renders; approved Call is adjacent. |
| RESEARCH | - | Pure local state machine and narrow client boundary | 03-12 | COVERED | Reset is explicit in transition contract and UI. |
| UI-SPEC | - | Focus management and truthful local confirmation | 03-12 | COVERED | Restored select receives focus; copy stays local-only. |
| REVIEW | CR-02 | Dead Start another inquiry action | 03-12 | COVERED | Same-fragment link is replaced by reset. |
</source_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL-normalized initial topic to mutable client draft | The allowlisted origin must survive visitor edits and reset predictably. |
| success state to new idle form | Stale personal data, errors, or loading state must not leak into the next inquiry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-44 | Tampering | InquiryForm reset topic | mitigate | Seed both origin and editable topic only from normalized initialTopic. |
| T-03-45 | Information Disclosure | reset draft | mitigate | Clear all visitor-entered fields and errors before rendering the next form. |
| T-03-46 | Denial of Service | success restart action | mitigate | Use a real button/state transition and restore keyboard focus to the first control. |
| T-03-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>

<verification>
<automated>node --test tests/inquiry-demo.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
</verification>

<success_criteria>
- Start another inquiry always restores a clean form and useful focus.
- Normalized origin/topic are preserved correctly across reset.
- Existing invalid/loading/duplicate/success behavior remains truthful.
- Approved Call remains available without replacing restart.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-12-SUMMARY.md` when done.</output>
