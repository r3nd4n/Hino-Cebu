---
phase: 03-truck-discovery-local-support-routes
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tests/support-routes.test.mjs
  - src/content/services.ts
  - src/content/about.ts
  - src/app/parts-service/page.tsx
  - src/app/about/page.tsx
autonomous: true
requirements: [DISC-03, DISC-04]
must_haves:
  truths:
    - "A visitor reaches a dedicated Parts & Service page led by separate Parts and Service paths, followed by quieter fleet-support and maintenance guidance (D-14, D-15)."
    - "A visitor reaches a concise About page ordered as local customer commitment, clearly separated Hino Motors Philippines background, practical Cebu facts, and conversion actions (D-14, D-16)."
    - "National background provenance remains internal and no national statement becomes a Cebu history, authorization, legal identity, inventory, or capability claim (D-17)."
    - "Parts, Service, and About retain both an allowlisted shared-contact inquiry route and the verified call route (D-10, D-13)."
  artifacts:
    - path: tests/support-routes.test.mjs
      provides: Wave 0 Parts/Service/About content and trust-boundary tests.
    - path: src/content/services.ts
      provides: Typed primary/supporting aftersales presentation records.
    - path: src/content/about.ts
      provides: Public local/national copy plus private corporate provenance.
    - path: src/app/parts-service/page.tsx
      provides: Two-primary-path aftersales route.
    - path: src/app/about/page.tsx
      provides: Concise separated local and national credibility route.
  key_links:
    - from: src/app/parts-service/page.tsx
      to: src/content/services.ts
      via: primary/supporting offering projections and typed inquiry topics.
      pattern: "serviceOfferings"
    - from: src/app/about/page.tsx
      to: src/content/about.ts
      via: public projection excluding corporate source metadata.
      pattern: "aboutContent|getPublicAboutContent"
    - from: src/app/parts-service/page.tsx
      to: src/content/inquiry.ts
      via: `parts` and `service` contextual CTA URLs.
      pattern: "inquiryHref"
---

## Phase Goal

**As a** Cebu business visitor, **I want to** understand local parts, service, and dealer support paths, **so that** I can choose the right verified way to contact Hino Cebu.

<objective>
Deliver distinct, practical Parts & Service and About routes using only configured local facts and internally sourced national background.

Purpose: extend conversion beyond vehicle pages while keeping support capabilities and dealer credibility truthful.
Output: Wave 0 support tests, typed support/about content, and complete `/parts-service` and `/about` routes.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-truck-discovery-local-support-routes/03-CONTEXT.md
@.planning/phases/03-truck-discovery-local-support-routes/03-RESEARCH.md
@.planning/phases/03-truck-discovery-local-support-routes/03-PATTERNS.md
@.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md
@.planning/phases/03-truck-discovery-local-support-routes/03-VALIDATION.md
@.planning/phases/03-truck-discovery-local-support-routes/03-01-SUMMARY.md

<interfaces>
From src/content/services.ts: existing `ServiceOffering { name, description, href }` and four records are the approved support-language base.
From src/content/site.ts: configured `phone`, `address`, `hours`, and approved claims are authoritative; unresolved legal identity/email/directions are not About claims.
From 03-01: `PageHero`, `LocalContactCta`, `InquiryTopic`, and `inquiryHref(topic)` are reusable public conversion contracts.
From src/content/assets.ts: `partsServiceWorkshop` is a generated internal replacement asset and cannot be described as the Cebu facility or proof of capability.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the failing local-support route contract</name>
  <read_first>
    <file>tests/support-routes.test.mjs (create if absent)</file>
    <file>tests/homepage.test.mjs</file>
    <file>src/content/services.ts</file>
    <file>src/content/site.ts</file>
    <file>src/components/homepage/HomepageSupportSections.tsx</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-VALIDATION.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>tests/support-routes.test.mjs</files>
  <behavior>
    - Parts and Service are the two primary paths and use distinct `parts`/`service` inquiry topics; Fleet support and Maintenance guidance follow as non-guaranteeing support information.
    - About visibly separates local commitment from the section titled `About Hino Motors Philippines` and closes with configured Cebu facts and contact actions.
    - Public support/About sources contain no corporate source URL/provenance, guessed contact value, national-to-local authorization claim, or Promotions surface.
  </behavior>
  <action>Create `tests/support-routes.test.mjs` using the established parallel file-read/source-contract pattern. Assert both route files exist with one `main#main-content`, correct section order, shared `PageHero`/`LocalContactCta`, Parts/Service contextual topics and CTAs, exactly two primary plus two supporting offerings, the exact national section title, configured phone/address/hours imports, and no local-history, legal-entity, authorization, inventory, guarantee, turnaround, mother-site URL, provenance identifier, generated-image-as-Cebu-facility claim, inactive unresolved anchor, or Promotions text. Keep Contact-specific assertions for Plan 03 so this RED contract can become green as a coherent support slice.</action>
  <acceptance_criteria>
    <criterion>`tests/support-routes.test.mjs` exists and fails on missing Phase 3 support/About artifacts.</criterion>
    <criterion>Assertions trace D-14 through D-17 and verify the shared inquiry/call contract from D-10/D-13.</criterion>
    <criterion>The test prevents corporate-source leakage and national-to-local claim transfer.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/support-routes.test.mjs</automated>
    <manual>Confirm failure is limited to the intentionally absent Parts/Service and About implementation.</manual>
  </verify>
  <done>A failing Wave 0 contract defines both local-support routes, their role separation, conversion actions, and claim-safety boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the distinct Parts & Service and About journeys</name>
  <read_first>
    <file>src/content/about.ts (create if absent)</file>
    <file>src/app/parts-service/page.tsx (create if absent)</file>
    <file>src/app/about/page.tsx (create if absent)</file>
    <file>tests/support-routes.test.mjs</file>
    <file>src/content/services.ts</file>
    <file>src/content/site.ts</file>
    <file>src/content/assets.ts</file>
    <file>src/components/shared/PageHero.tsx</file>
    <file>src/components/shared/LocalContactCta.tsx</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-RESEARCH.md</file>
    <file>.planning/phases/03-truck-discovery-local-support-routes/03-UI-SPEC.md</file>
  </read_first>
  <files>src/content/services.ts, src/content/about.ts, src/app/parts-service/page.tsx, src/app/about/page.tsx</files>
  <behavior>
    - Visitors can choose Parts or Service directly before seeing fleet/maintenance guidance.
    - Visitors can understand the local customer commitment without being shown unsupported dealership biography or credentials.
    - Each route offers an inquiry-first action and an adjacent configured Call action.
  </behavior>
  <action>Extend `ServiceOffering` with `role: "primary" | "supporting"`, approved bullets, an `InquiryTopic`, CTA label, and stable section id while preserving the four existing offerings. Keep copy non-guaranteeing and do not imply stock, service plans, uptime, inventory, turnaround, or facility capabilities. Create `src/content/about.ts` with a public local-commitment/practical-support record and a separate national background record limited to the research-approved substance: Hino Motors Philippines Corporation was established in March 1975 and the official source describes assembly/distribution of Hino trucks, buses, and spare parts alongside parts and maintenance services. Store `https://www.hino.com.ph/corporate-information`, publisher, review date, and supported fields only in private provenance and export a public projection without them. Implement `/parts-service` per D-14/D-15 with Parts then Service primary cards, 2â€“3 configured bullets, precise `parts`/`service` CTAs, then quieter Fleet/Maintenance cards, and a general conversion panel. Implement `/about` per D-14/D-16/D-17 in the exact local commitment â†’ `About Hino Motors Philippines` â†’ practical Cebu facts â†’ conversion order. Source phone/address/hours only from `siteConfig`; inquiry is primary and verified Call remains adjacent. Do not render or imply local history, legal entity, authorization, staff, awards, territory, inventory, customer volume, dealer count, or national source links. Leave the generated workshop asset unused unless it is already accepted and, if used, expose only public image fields and never call it a Cebu facility.</action>
  <acceptance_criteria>
    <criterion>`/parts-service` contains two equal primary paths in Parts-then-Service order and a subordinate two-card Fleet/Maintenance band.</criterion>
    <criterion>Parts and Service actions use only `inquiryHref("parts")` and `inquiryHref("service")`; every page also exposes `siteConfig.contact.phone.href`.</criterion>
    <criterion>`/about` has the required four-part concise order and visually/semantically separates national background from local commitment.</criterion>
    <criterion>Corporate provenance stays in `src/content/about.ts` and is absent from public component props/JSX.</criterion>
    <criterion>No visible claim exceeds configured support facts or transfers a national fact to Hino Cebu.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/support-routes.test.mjs</automated>
    <manual>Read the local and national About sections as separate subjects; confirm no sentence can be reasonably read as Cebu dealer history, authorization, inventory, or legal identity.</manual>
  </verify>
  <done>Parts & Service and About are useful, distinct, configurable, locally actionable, and free of unsupported dealer/capability claims or public source links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| official corporate source â†’ public About projection | Only reviewed national background may render; source metadata stays private. |
| configured support copy â†’ perceived local capability | General support language must not become inventory, uptime, facility, or turnaround claims. |
| support CTA topic â†’ Contact route | Only `parts`, `service`, or `general` typed keys may be emitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-05 | Information Disclosure | `src/content/about.ts` projection | mitigate | Keep URL/publisher/review/supports in private metadata and export public copy separately; assert absence from routes. |
| T-03-06 | Spoofing | About local identity claims | mitigate | Consume only `siteConfig` facts and prohibit local legal identity, history, authorization, awards, inventory, and territory claims. |
| T-03-07 | Tampering | service-capability presentation | mitigate | Render immutable approved offerings; prohibit guarantees and prevent generated workshop media from being labeled as local evidence. |
| T-03-SC | Tampering | npm installs | accept | No package installation occurs in this plan. |
</threat_model>

<verification>
<automated>node --test tests/discovery-routes.test.mjs tests/support-routes.test.mjs</automated>
<automated>npm run lint</automated>
</verification>

<success_criteria>
- Parts & Service clearly separates two primary assistance paths from supporting guidance.
- About is concise, correctly separates national and local subjects, and uses only verified/configured Cebu facts.
- Both routes preserve contextual inquiry and verified call actions with no visible promotions.
</success_criteria>

<output>Create `.planning/phases/03-truck-discovery-local-support-routes/03-02-SUMMARY.md` when done.</output>
