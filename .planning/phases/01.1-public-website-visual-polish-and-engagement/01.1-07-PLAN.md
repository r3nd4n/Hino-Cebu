---
phase: 01.1-public-website-visual-polish-and-engagement
plan: "07"
type: execute
wave: 4
depends_on: [01.1-04]
files_modified:
  - src/lib/marketing-consent.ts
  - src/components/marketing/MarketingTags.tsx
  - src/lib/analytics.ts
  - src/app/(public)/layout.tsx
autonomous: true
requirements: [GOV-10]
must_haves:
  truths:
    - "Missing, malformed, stale-version, denied, or insufficient consent leaves optional GTM, GA4, and Meta provider code and network requests unloaded."
    - "Analytics and advertising grants are purpose-specific; GTM loads only under the conservative both-grants policy, GA4 requires analytics consent, and Meta requires advertising consent."
    - "The public shell and typed analytics boundary use one fail-closed consent contract without adding the Phase 3 consent UI or claiming GOV-05/GOV-06 completion."
  artifacts:
    - path: src/lib/marketing-consent.ts
      provides: "Versioned granular consent parser, denied default, and provider-bootstrap contract"
      exports: [parseMarketingConsent, buildMarketingTagBootstrap]
    - path: src/components/marketing/MarketingTags.tsx
      provides: "Single first-party consent gate for optional marketing providers"
    - path: src/lib/analytics.ts
      provides: "Purpose-gated typed analytics dispatch"
  key_links:
    - from: src/components/marketing/MarketingTags.tsx
      to: src/lib/marketing-consent.ts
      via: "generated fail-closed bootstrap"
      pattern: "buildMarketingTagBootstrap"
    - from: src/app/(public)/layout.tsx
      to: src/components/marketing/MarketingTags.tsx
      via: "sole public-shell tag-control boundary"
      pattern: "<MarketingTags"
    - from: src/lib/analytics.ts
      to: src/lib/marketing-consent.ts
      via: "purpose check before provider dispatch"
      pattern: "getStoredMarketingConsent|parseMarketingConsent"
---

<objective>
Make optional marketing loading fail closed behind an explicit, granular consent contract.

Purpose: Close the consent-aware half of GOV-10 without preempting Phase 3's full GOV-05/GOV-06 consent UI, withdrawal experience, policy governance, or approved tag administration.
Output: A versioned consent helper, one gated marketing-tag boundary, and consent-aware typed event dispatch.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-CONTEXT.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-VALIDATION.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-04-SUMMARY.md

<interfaces>
From src/components/marketing/MarketingTags.tsx:
`export function MarketingTags(): React.ReactNode` currently reads `NEXT_PUBLIC_GTM_ID`, `NEXT_PUBLIC_GA4_ID`, and `NEXT_PUBLIC_META_PIXEL_ID` and renders provider scripts directly.

From src/lib/analytics.ts:
`export function track(event: AnalyticsEvent, properties?: AnalyticsProperties): void` is the sole typed browser event boundary.

From src/app/(public)/layout.tsx:
`PublicLayout` renders exactly one `<MarketingTags />` within the governed public shell; root layout must remain tag-free.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the versioned consent contract and gated provider bootstrap</name>
  <files>src/lib/marketing-consent.ts, src/components/marketing/MarketingTags.tsx</files>
  <read_first>
    src/components/marketing/MarketingTags.tsx
    src/lib/analytics.ts
    src/app/(public)/layout.tsx
    src/components/marketing/AttributionCapture.tsx
    tests/governance.test.mjs
    tests/visual-polish.test.mjs
    .planning/REQUIREMENTS.md
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Missing storage, malformed JSON, wrong version, unknown values, or explicit denial returns `{ analytics: false, advertising: false }` and injects no third-party script.
    - GA4 is eligible only with analytics consent, Meta only with advertising consent, and generic GTM only when both purposes are granted because its downstream tag mix is not yet governed.
    - Invalid or missing provider identifiers fail closed; generated values are safely encoded and no raw environment value becomes executable script syntax.
  </behavior>
  <action>Per GOV-10, create a browser-safe, dependency-free `src/lib/marketing-consent.ts` with a stable storage key, explicit schema version, closed `analytics`/`advertising` grant states, a strict parser, and an immutable denied default. Export a deterministic `buildMarketingTagBootstrap` that checks the same versioned local preference before creating any optional provider element or provider global. Refactor `MarketingTags` to remain the sole public tag-control component and emit only the small first-party gate when at least one syntactically valid configured identifier exists; remove unconditional provider `<Script src>` and initialization blocks. The gate must load GA4 only for analytics grant, Meta only for advertising grant, and GTM only when both grants are true. Missing/unapproved consent must leave `googletagmanager.com` and `connect.facebook.net` requests absent. Validate provider ID shapes and serialize data safely. Do not add a banner, settings UI, default opt-in, consent-mode ping, cookie sync, provider dashboard policy, or approval workflow: Phase 3 retains complete GOV-05/GOV-06 consent UX and analytics governance.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P07-T01:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - P07-T01 executes the bootstrap in isolated Node `vm` fixtures and proves zero provider insertion/globals for absent, malformed, stale-version, denied, and purpose-insufficient states.
    - Analytics-only consent can load valid GA4 but not Meta or GTM; advertising-only can load valid Meta but not GA4 or GTM; both grants are required for valid GTM.
    - No provider `src` is rendered directly by MarketingTags before the gate and malformed IDs produce no provider URL.
    - The helper contains no PII, provider SDK, package dependency, or implicit granted default.
  </acceptance_criteria>
  <done>Optional provider loading has one testable, granular, fail-closed boundary with explicit conservative GTM semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire consent-aware shell and analytics dispatch</name>
  <files>src/lib/analytics.ts, src/app/(public)/layout.tsx</files>
  <read_first>
    src/lib/analytics.ts
    src/lib/marketing-consent.ts
    src/app/(public)/layout.tsx
    src/app/layout.tsx
    src/components/marketing/MarketingTags.tsx
    src/components/marketing/TrackedLink.tsx
    tests/governance.test.mjs
    tests/visual-polish.test.mjs
  </read_first>
  <behavior>
    - Typed events dispatch to dataLayer/gtag only with analytics consent and to fbq only with advertising consent.
    - Missing or invalid consent causes `track` to return without creating globals, storage, provider calls, or network behavior.
    - MarketingTags remains present exactly once in the public server layout and absent from the root layout and all other routes/components.
  </behavior>
  <action>Update `track` to read the shared strict consent contract before touching optional analytics globals: analytics grant gates `dataLayer` and `gtag`, advertising grant gates `fbq`, and no grant returns safely. Preserve the existing typed event allow-list and primitive/undefined filtering; do not add PII, persist event payloads, or change attribution behavior. Keep exactly one `MarketingTags` composition in `src/app/(public)/layout.tsx`, after the existing public shell content, and preserve the root/public ownership tests and Server Component shell. Do not add consent state to Header or any visual component. Document in identifiers/actionable comments only where necessary that Phase 3 will provide the user-facing set/revisit/withdraw flow against this contract.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P07-T02:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - P07-T02 proves missing/denied consent produces no dataLayer, gtag, or fbq call and granular grants dispatch only to their allowed purpose.
    - The existing `AnalyticsEvent` union and safe primitive-property filtering remain intact.
    - Exactly one public MarketingTags owner exists; root layout stays minimal and no new React client island is introduced.
    - No task claims completion of GOV-05, GOV-06, QUAL-03, or browser performance certification.
  </acceptance_criteria>
  <done>Both optional provider loading and event dispatch fail closed from one shared consent contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser preference to provider bootstrap | Local preference data is untrusted and must parse to denial unless exact and current. |
| public environment identifier to executable script | Configured IDs must be shape-checked and safely serialized before URL construction. |
| application event to optional provider globals | Typed event payloads must remain PII-free and purpose-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-23 | Tampering | consent preference | mitigate | Exact version/type parser with immutable denied fallback; reject malformed and unknown fields. |
| T-01.1-24 | Information Disclosure | optional provider loading | mitigate | Create no provider element/global until the required purpose grant is present. |
| T-01.1-25 | Elevation of Privilege | provider identifiers/bootstrap | mitigate | Allow-list identifier shapes, encode URL components, and serialize values without interpolation into executable syntax. |
| T-01.1-26 | Information Disclosure | analytics dispatch | mitigate | Preserve typed event names and primitive safe properties; gate by purpose and never persist payloads. |
| T-01.1-SC | Tampering | dependency surface | accept | Native browser/React/Next facilities only; no package install occurs. |
</threat_model>

<verification>
Run the exact `^P07-T01:` and `^P07-T02:` commands. Confirm tests exercise negative consent states, purpose separation, valid grants, single-boundary ownership, and no new dependency/client island.
</verification>

<success_criteria>
- Optional GTM, GA4, and Meta code/network activity is absent unless the exact required consent grant exists.
- Typed analytics dispatch uses the same fail-closed purpose decision.
- Phase 3 remains the explicit owner of the complete GOV-05/GOV-06 UI, withdrawal, policy, and tag-governance work.
</success_criteria>

<output>
Create `.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-07-SUMMARY.md` when done.
</output>
