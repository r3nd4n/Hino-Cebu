---
phase: 01.1-public-website-visual-polish-and-engagement
plan: "04"
type: execute
wave: 3
depends_on: [01.1-02, 01.1-03]
files_modified:
  - src/app/(public)/layout.tsx
  - src/app/globals.css
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/StickyMobileActions.tsx
  - SOURCE_REGISTER.md
autonomous: true
requirements: [GOV-10]
must_haves:
  truths:
    - "The official Hino mark leads a 72px task-first header, Cebu remains subordinate, and the phone utility strip is absent (D-02, D-06, D-07)."
    - "Current, hover, focus, mobile disclosure, Escape/focus-return, and reduced-motion behaviors are semantic and deterministic (D-08, D-09)."
    - "The footer alone contains honest social shells and the approved conversion-focused four/two/one-column information architecture (D-15 through D-18)."
    - "Mobile actions show up to three eligible actions in exact Call, Quote, Directions order without reconstructed destinations (D-06, D-18)."
  artifacts:
    - path: src/app/(public)/layout.tsx
      provides: "Server-produced shell DTO composition"
    - path: src/components/layout/Header.tsx
      provides: "Accessible task-first desktop/mobile navigation"
    - path: src/components/layout/Footer.tsx
      provides: "Governed footer and social state rendering"
    - path: src/components/layout/StickyMobileActions.tsx
      provides: "Eligible mobile conversion actions"
  key_links:
    - from: src/app/(public)/layout.tsx
      to: src/lib/governance/public-shell.ts
      via: "canonical server-only navigation projection"
      pattern: "getPublicShellNavigation"
    - from: src/app/(public)/layout.tsx
      to: src/components/layout/Footer.tsx
      via: "narrow social/branch/action DTO props"
      pattern: "socialProfiles"
    - from: src/components/layout/Header.tsx
      to: src/app/globals.css
      via: "72px disclosure and semantic current/focus styles"
      pattern: "site-header|primary-navigation"
---

<objective>
Deliver the complete governed public shell: task-first header, accessible disclosure navigation, conversion footer, honest social states, and eligible mobile actions.

Purpose: Make the site easier to understand and act on while preserving the server eligibility boundary.
Output: Refined public layout and shell components with responsive, accessible behavior.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-CONTEXT.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-PATTERNS.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-02-SUMMARY.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire final eligible shell DTOs at the public server layout</name>
  <files>src/app/(public)/layout.tsx</files>
  <read_first>
    src/app/(public)/layout.tsx
    src/lib/governance/public-shell.ts
    src/content/site.ts
    src/components/layout/Header.tsx
    src/components/layout/Footer.tsx
    src/components/layout/StickyMobileActions.tsx
    tests/governance.test.mjs
    tests/visual-polish.test.mjs
  </read_first>
  <action>Replace the one-label local navigation mapping with `getPublicShellNavigation()` from Plan 02 and project its already-safe output into the Header, Footer, and StickyMobileActions contracts. Project independently eligible branch identity/address/hours, canonical phone/directions actions, eligible legal-route items, and `getEligibleSocialProfiles()` results into narrow readonly props. Keep the public layout a Server Component; do not pass raw `siteConfig.socials`, governance records, claims, approval envelopes, evidence, withheld reasons, or pending values. Feed the same quote/contact/navigation DTO instances to all shell consumers so destinations cannot drift.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P04-T01:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Public layout imports and invokes the Plan 02 canonical shell/social selectors.
    - Header, Footer, and StickyMobileActions receive only readonly public DTOs derived once on the server.
    - No client file imports `siteConfig`, governance records, eligibility selectors, evidence, or approval internals.
    - Withheld items are absent and never replaced with inert route labels or reconstructed literal destinations.
  </acceptance_criteria>
  <done>Every shell surface consumes one coherent fail-closed server projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the 72px accessible task-first header</name>
  <files>src/components/layout/Header.tsx, src/app/globals.css</files>
  <read_first>
    src/components/layout/Header.tsx
    src/app/globals.css
    src/lib/governance/public-shell.ts
    tests/visual-polish.test.mjs
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Desktop shows eligible ordered navigation and red Get a Quote action at 1060px and above; withheld items leave no gap.
    - Descendant routes such as truck details keep the parent item current with `aria-current="page"` and a persistent underline.
    - Below 1060px the 44x44 disclosure updates `aria-expanded`; selecting a link closes it; Escape closes it and returns focus to the trigger; closed links are not tabbable.
  </behavior>
  <action>Keep `Header` as the existing client island. Remove the utility phone strip and deprecated logo `priority`; render the approved 124x30 Hino logo with neutral 14px/700 Cebu descriptor. Implement a root-safe `isCurrentPath` descendant matcher. Render ordinary links and a native disclosure button, not ARIA menu roles or a focus trap. Add a trigger ref and Escape listener only while open, returning focus after close. Include eligible phone access in the mobile panel from the supplied contact DTOs, with Quote last among primary navigation actions. Style a sticky 72px header, 3px current underline, neutral hover, visible focus, 48px mobile rows, panel offset 72px, 200ms/8px maximum transition, and immediate reduced-motion state changes.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P04-T02:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - No utility strip markup or 106px offset remains.
    - Logo dimensions are 124x30, Cebu is subordinate, and neither logo nor menu adds image preload.
    - `aria-current` handles exact and descendant routes without making `/` match everything.
    - Escape returns focus to the menu trigger, closed links cannot be tabbed, and no focus trap/client island is added.
    - Focus indicator remains visible over white, charcoal, and red action surfaces.
  </acceptance_criteria>
  <done>Desktop and mobile navigation meet D-02 and D-06 through D-09 with semantic keyboard behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the governed conversion footer and mobile action bar</name>
  <files>src/components/layout/Footer.tsx, src/components/layout/StickyMobileActions.tsx, src/app/globals.css, SOURCE_REGISTER.md</files>
  <read_first>
    src/components/layout/Footer.tsx
    src/components/layout/StickyMobileActions.tsx
    src/components/marketing/TrackedLink.tsx
    src/app/globals.css
    SOURCE_REGISTER.md
    src/content/site.ts
    tests/visual-polish.test.mjs
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Unverified social profiles render muted repository-owned icons and fixed copy with no anchor, button, tab stop, hover, or pointer behavior.
    - Verified profiles render 44x44 links with `_blank`, `noopener noreferrer`, and an accessible new-tab label; withheld destination output has no href.
    - Footer columns collapse 4 to 2 to 1; mobile bar shows eligible Call, Quote, Directions in exact order and actual-count columns.
  </behavior>
  <action>Keep Footer server-compatible and DTO-only. Render the official logo plus subordinate Cebu, independently eligible branch address/phone/hours/directions, customer paths, support/contact, eligible legal routes, one red `Get a Quote` CTA, and the social row in the required four-column composition. Add small repository-owned inline Facebook/YouTube/Instagram SVGs from official platform brand resources and record their source/provenance in `SOURCE_REGISTER.md`; set decorative SVGs `aria-hidden`. Use the exact unavailable social heading/support copy. For verified variants only, render safe external links with exact attributes and accessible labels. Update StickyMobileActions to preserve `TrackedLink` analytics while selecting at most Call Hino Cebu, Get a Quote, Get Directions in that order from supplied eligible DTOs. Style footer breakpoints and the 66px bar with safe-area inset, actual-count grid, two-line centered labels, 44px targets, and matching body bottom clearance.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P04-T03:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Footer contains only eligible branch/contact/navigation/legal values and exact fixed social unavailable copy.
    - Unverified/withheld social states contain no interactive semantics; verified links have `_blank`, `rel="noopener noreferrer"`, and new-tab accessible names.
    - Platform SVG provenance is recorded and no icon package, feed, embed, SDK, or third-party script is added.
    - Mobile actions preserve analytics, exact visible labels/order, actual eligible count, safe-area clearance, and 44px targets.
  </acceptance_criteria>
  <done>The footer and mobile bar provide honest, governed conversion paths at every breakpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server layout to client Header | Only final eligible DTO fields may cross into the interactive boundary. |
| verified social DTO to external browser origin | New-tab navigation must be host-validated upstream and opener-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-09 | Information Disclosure | shell props | mitigate | Pass stable labels/hrefs/status only; forbid records, diagnostics, and evidence in client sources. |
| T-01.1-10 | Spoofing | Footer social links | mitigate | Render href only from verified DTO and require `_blank` plus `rel="noopener noreferrer"`. |
| T-01.1-11 | Tampering | navigation/contact actions | mitigate | Reuse one server projection and never reconstruct destinations inside components. |
| T-01.1-12 | Denial of Service | social/footer runtime | mitigate | Static inline SVG only; no feeds, scripts, SDKs, or new client state. |
| T-01.1-SC | Tampering | package surface | mitigate | Source contract blocks package/icon/animation/social SDK additions. |
</threat_model>

<verification>
Run only the exact `^P04-T01:`, `^P04-T02:`, and `^P04-T03:` commands attached to their owning tasks; defer the full suite and production build to Plan 07.
</verification>

<success_criteria>
- Desktop/mobile shell behavior matches the approved order, semantics, dimensions, and breakpoints.
- Social activation remains fail-closed and opener-safe.
- The shell adds no runtime dependency, feed, script, or client island.
</success_criteria>

<output>
Create `.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-04-SUMMARY.md` when done.
</output>
