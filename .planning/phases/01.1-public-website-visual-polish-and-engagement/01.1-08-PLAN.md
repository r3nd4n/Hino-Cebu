---
phase: 01.1-public-website-visual-polish-and-engagement
plan: "08"
type: execute
wave: 4
depends_on: [01.1-04, 01.1-05, 01.1-06]
files_modified:
  - performance-budgets.json
  - scripts/check-performance-budgets.mjs
  - package.json
autonomous: true
requirements: [GOV-10]
must_haves:
  truths:
    - "Repository and CI fail when explicit mobile-protective source or production-build budgets are exceeded."
    - "Budgets measure emitted gzip JavaScript/CSS, the largest client chunk, client-boundary count, homepage image-preload count, and registered official source-image size without claiming browser LCP, CLS, INP, or per-route transfer evidence."
    - "Budget enforcement uses Node built-ins and the existing production build, with zero new dependency."
  artifacts:
    - path: performance-budgets.json
      provides: "Reviewed numeric GOV-10 thresholds"
      contains: "maxTotalClientJsGzipBytes"
    - path: scripts/check-performance-budgets.mjs
      provides: "Dependency-free source/build-output budget gate"
    - path: package.json
      provides: "CI-equivalent check chain that runs budgets after next build"
      contains: "performance:budget"
  key_links:
    - from: scripts/check-performance-budgets.mjs
      to: performance-budgets.json
      via: "strict numeric budget input"
      pattern: "performance-budgets.json"
    - from: package.json
      to: scripts/check-performance-budgets.mjs
      via: "post-build performance:budget script"
      pattern: "npm run build && npm run performance:budget"
---

<objective>
Add explicit, measurable, dependency-free performance budgets to the repository and its complete check gate.

Purpose: Close the enforceable-budget half of GOV-10 while reserving real-device/browser metric certification for later release verification.
Output: Numeric budget configuration, a deterministic checker, and CI-equivalent package wiring.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-VALIDATION.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-04-SUMMARY.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-05-SUMMARY.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-06-SUMMARY.md

<interfaces>
From package.json:
`check` currently runs lint, typecheck, native tests, then `next build`; the budget gate must run only after that successful build.

From the current Next.js production output:
client JavaScript and CSS are emitted below `.next/static/chunks/`; the checker must discover hashed filenames rather than assume names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce numeric source and build-output budgets in the complete check</name>
  <files>performance-budgets.json, scripts/check-performance-budgets.mjs, package.json</files>
  <read_first>
    package.json
    next.config.ts
    src/app/layout.tsx
    src/app/(public)/page.tsx
    src/components/marketing/MarketingTags.tsx
    tests/visual-polish.test.mjs
    .planning/REQUIREMENTS.md
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Missing/malformed config, missing production output, a nonnumeric threshold, or any measured value above budget exits nonzero with the metric, actual value, and limit.
    - A normal production build passes all explicit thresholds; an isolated fixture exceeding each threshold fails deterministically.
    - `npm run check` runs the budget gate after `next build`, so stale or absent `.next` output cannot be accepted.
  </behavior>
  <action>Per GOV-10, add `performance-budgets.json` with these explicit thresholds: total gzip size of all emitted `.next/static/chunks/**/*.js` at most 260000 bytes; largest single emitted JavaScript chunk gzip size at most 90000 bytes; total gzip size of emitted `.next/static/chunks/**/*.css` at most 20000 bytes; at most 5 exact `"use client"` TypeScript/TSX modules under `src` (do not count `"use server"`); exactly 1 `preload` image usage in application source; and each registered raster under `public/images/official` at most 1050000 source bytes. Implement `scripts/check-performance-budgets.mjs` using only `node:fs`, `node:path`, and `node:zlib`; recursively discover hashed output, gzip actual bytes, reject missing/empty expected output, validate every config key, print a compact metric table, and exit nonzero on any breach. Tests must run the checker against temporary fixture roots through an explicit CLI root argument so every failure mode is exercised without mutating real build output. Add `performance:budget` and change `check` to `npm run lint && npm run typecheck && npm test && npm run build && npm run performance:budget`. Do not install Lighthouse, bundle analyzers, browser automation, or another package. State in script/config documentation that these are enforceable repository/build-output proxies: Phase 4/5 browser certification still owns per-route request/transfer inspection and observed LCP, CLS, and INP on protected preview/real devices, so this plan must not assert those browser metrics.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P08-T01:" tests/visual-polish.test.mjs &amp;&amp; npm run build &amp;&amp; npm run performance:budget</automated>
  </verify>
  <acceptance_criteria>
    - All six numeric contracts exist exactly and the checker reports actual versus limit for each.
    - Fixture tests prove over-budget JS total, largest chunk, CSS total, client-module count, preload count, official-image size, malformed config, and missing build output all fail closed.
    - The real post-build gate passes and `npm run check` invokes it only after `npm run build`.
    - No dependency or lockfile change occurs, and no result is described as measured LCP, CLS, INP, or complete per-route network certification.
  </acceptance_criteria>
  <done>GOV-10 has a deterministic CI-equivalent numeric budget gate with explicit scope and honest browser-certification limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| budget configuration to release gate | Malformed or weakened thresholds must not silently disable enforcement. |
| hashed production output to measurement | The checker must discover and measure actual emitted files, not stale named examples. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-27 | Tampering | performance-budgets.json | mitigate | Require every fixed key as a positive integer and fail on missing/unknown-invalid configuration. |
| T-01.1-28 | Repudiation | budget report | mitigate | Print metric, actual bytes/count, threshold, and PASS/FAIL for every run. |
| T-01.1-29 | Denial of Service | public client/build payload | mitigate | Block the complete check on gzip JS/CSS, largest chunk, client boundary, preload, or source-image breach. |
| T-01.1-SC | Tampering | package surface | mitigate | Node built-ins only; package-lock must remain unchanged and tests assert no new dependency. |
</threat_model>

<verification>
Run the exact P08-T01 contract, a fresh production build, and the budget checker. Then inspect `package.json` to confirm the complete check runs budgets after build and no dependency changed.
</verification>

<success_criteria>
- Every agreed numeric source/build-output threshold is enforceable locally and in the repository's CI-equivalent check.
- Failures identify the breached metric and cannot pass on absent/stale output.
- Later browser certification retains ownership of actual per-route transfer/request behavior and observed LCP, CLS, and INP.
</success_criteria>

<output>
Create `.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-08-SUMMARY.md` when done.
</output>
