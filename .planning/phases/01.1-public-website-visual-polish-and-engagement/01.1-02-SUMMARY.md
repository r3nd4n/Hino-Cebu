---
phase: 01.1-public-website-visual-polish-and-engagement
plan: "02"
subsystem: governance
tags: [server-dto, navigation, social-profiles, url-validation, fail-closed]

requires:
  - phase: 01-production-contracts-and-executable-configuration
    provides: approval currency checks, final route eligibility, branch actions, and eligible truck selectors
  - phase: 01.1-public-website-visual-polish-and-engagement
    provides: Wave 0 task-prefixed visual-polish contracts
provides:
  - Typed unverified, verified, and withheld social-profile public states
  - Approval-, HTTPS-, and host-gated social destination projection
  - Canonical task-first public navigation derived from final eligible prerequisites
affects: [01.1-04, public-layout, header, footer]

tech-stack:
  added: []
  patterns: [private governance record to narrow public DTO, derived fail-closed navigation prerequisites]

key-files:
  created:
    - src/lib/governance/public-shell.ts
  modified:
    - src/content/site.ts
    - src/app/(public)/layout.tsx

key-decisions:
  - "Unverified and withheld social states carry only platform, label, and status; only a currently approved HTTPS platform URL can produce href."
  - "Find Your Truck requires both the eligible Trucks parent and at least one eligible truck, while Hino Cebu requires eligible branch identity plus a public branch action."

patterns-established:
  - "External profile projection: validate approval currency, HTTPS scheme, and exact platform host before returning a browser destination."
  - "Shell navigation: project ordered public DTOs from final eligibility selectors and omit failed prerequisites without placeholders."

requirements-completed: [GOV-09, GOV-10]

duration: 6 min
completed: 2026-08-22
---

# Phase 01.1 Plan 02: Governed Public Shell Contracts Summary

**Fail-closed social profiles and task-first navigation now cross the server boundary only as narrow, prerequisite-backed public DTOs.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-22T10:38:47Z
- **Completed:** 2026-08-22T10:44:02Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added closed Facebook, YouTube, and Instagram states whose default unverified projections contain no national URLs or clickable destinations.
- Restricted verified social destinations to current brand-content approval, HTTPS, and an exact per-platform hostname allow-list while stripping all private governance fields.
- Added the exact D-07 navigation order and derived Find Your Truck and Hino Cebu eligibility from canonical route, truck, branch, and contact-action selectors.

## Task Commits

Each task was committed atomically:

1. **Task 1: Model and validate governed social-profile states** - `f3ffd5e` (feat)
2. **Task 2: Resolve canonical task-first shell destinations** - `537c9d8` (feat)

## Files Created/Modified

- `src/content/site.ts` - Defines private social records, discriminated public DTOs, default unverified profiles, and the verified destination validator.
- `src/lib/governance/public-shell.ts` - Produces the ordered public navigation DTO from final eligible routes and derived prerequisites.
- `src/app/(public)/layout.tsx` - Consumes the canonical server navigation projection at the public shell boundary.

## Decisions Made

- Kept the existing national `siteConfig.socials` values outside the new selector; default Hino Cebu social output is three non-clickable unverified records.
- Used exact host allow-lists rather than suffix matching so lookalike or cross-platform hosts fail closed.
- Treated derived destinations as independent stable navigation IDs while retaining Quote as the final primary action.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Wired the new selector into the public server layout**
- **Found during:** Task 2 (Resolve canonical task-first shell destinations)
- **Issue:** The Wave 0 P02-T02 acceptance contract verifies the canonical ordering at the public-layout boundary, while the task file list named only the new selector module.
- **Fix:** Replaced the layout's single-route projection with `getPublicShellNavigation()` and retained a fail-closed canonical label boundary.
- **Files modified:** `src/app/(public)/layout.tsx`
- **Verification:** `node --test --test-name-pattern="^P02-T02:" tests/visual-polish.test.mjs`, `npm run typecheck`, and `npm run lint` pass.
- **Committed in:** `537c9d8`

---

**Total deviations:** 1 auto-fixed (1 blocking).
**Impact on plan:** The adjustment completes the intended server-only integration boundary without adding client governance imports or broadening public data.

## Issues Encountered

- The plan's Task 1 read list referenced `src/lib/governance/approval.ts`, which does not exist. The existing canonical approval primitives are in `src/lib/governance/schemas.ts` and `src/lib/governance/eligibility.ts`; those sources were used without introducing a duplicate module.
- The globally installed `gsd-sdk` executable exposes a different CLI surface, so state tracking uses the repository-compatible `gsd-tools.cjs` fallback.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None. The three default `unverified` social states are intentional governed output, not placeholder links.

## Validation Results

- `node --test --test-name-pattern="^P02-T01:" tests/visual-polish.test.mjs` - PASS.
- `node --test --test-name-pattern="^P02-T02:" tests/visual-polish.test.mjs` - PASS.
- `npm run typecheck` - PASS.
- `npm run lint` - PASS.
- The Wave 0 RED meta-runner was not rerun, as required.

## Next Phase Readiness

- Plan 04 can render the social DTOs in the footer and reuse the canonical navigation DTO in responsive shell components.
- Verified social links remain unavailable until approved Hino Cebu profile URLs are recorded with current approval.
- Existing route and product publication blockers remain fail-closed and unchanged.

## Self-Check: PASSED

- All three declared created or modified files exist.
- Task commits `f3ffd5e` and `537c9d8` exist in repository history.
- Both plan-owned focused contracts, strict typecheck, and lint are green.

---
*Phase: 01.1-public-website-visual-polish-and-engagement*
*Completed: 2026-08-22*
