---
phase: 01.1-public-website-visual-polish-and-engagement
plan: "03"
subsystem: ui
tags: [next-font, barlow, design-tokens, responsive-media, accessibility]

requires:
  - phase: 01.1-public-website-visual-polish-and-engagement
    provides: Wave 0 P03 source contracts and approved UI specification
provides:
  - Root-level self-hosted Barlow 400/700 typography boundary
  - Exact public spacing, color, radius, shadow, motion, focus, and responsive tokens
  - Optional eligible-media PageHero with dark/light variants and stable image geometry
affects: [01.1-04, 01.1-05, 01.1-06, public-shell, route-composition]

tech-stack:
  added: []
  patterns: [single root next-font instance, CSS-token visual system, optional server-rendered eligible media]

key-files:
  created: []
  modified:
    - src/app/layout.tsx
    - src/app/globals.css
    - src/components/ui/Shared.tsx
    - src/app/(public)/hino-cebu/page.tsx

key-decisions:
  - "Barlow is instantiated once at the root with only Latin normal 400/700 assets and exposed through a CSS variable with Arial and sans-serif fallbacks."
  - "PageHero remains a named Server Component and renders media only when a narrow eligible DTO is supplied; text-only output remains complete."

patterns-established:
  - "Public typography uses only body 16/400, label 14/700, heading 24/700, and display 48/700 roles outside the scoped approval report."
  - "Responsive media reserves 3:2 geometry, supplies contextual alt text and sizes, and remains lazy unless a later route explicitly owns the sole homepage preload."

requirements-completed: [GOV-09, GOV-10]

duration: 20 min
completed: 2026-08-22
---

# Phase 01.1 Plan 03: Visual Foundation and Shared Media Summary

**Self-hosted Barlow typography, exact premium-industrial design tokens, focus-safe motion primitives, and an optional responsive-media server hero now form the public visual foundation.**

## Performance

- **Duration:** 20 min
- **Started:** 2026-08-22T10:39:00Z
- **Completed:** 2026-08-22T10:54:00Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added one root `next/font/google` Barlow instance with Latin, normal style, swap display, and only the approved 400/700 weights.
- Replaced the public visual tokens with the exact spacing, color, radius, shadow, responsive-gutter, safe-area, motion, reduced-motion, and focus-separation contracts.
- Extended `PageHero` in place with optional eligible media, dark/light variants, a two-action ceiling, stable 3:2 geometry, responsive sizes, and complete text-only rendering.
- Normalized shared card media/body/action layout and removed the simulated branch image from the Hino Cebu route.

## Task Commits

Each task was committed atomically:

1. **Task 1: Apply the lean Barlow root typography contract** - `62b060e` (feat)
2. **Task 2: Replace public visual tokens and accessibility primitives** - `f1c65b9` (feat)
3. **Task 3: Extend shared server primitives for eligible media** - `ca82951` (feat)

**Post-wave integration fix:** `d385fe1` (fix) restores the approval report's existing inclusive responsive boundary contract.

## Files Created/Modified

- `src/app/layout.tsx` - Owns the single self-hosted Barlow configuration without absorbing the public shell.
- `src/app/globals.css` - Defines the approved public tokens, four-role typography, responsive grids/media, focus separation, and reduced-motion behavior.
- `src/components/ui/Shared.tsx` - Adds the narrow `EligibleHeroMedia` DTO, optional hero media, dark/light variants, and the exact default `Get a Quote` label.
- `src/app/(public)/hino-cebu/page.tsx` - Replaces simulated branch imagery with a truthful structured text panel.

## Decisions Made

- Applied Barlow through a root CSS variable so all public controls inherit one font instance while the root remains only the HTML/body/font boundary.
- Preserved the approval-report typography and legacy visual values through scoped custom-property overrides instead of sweeping the public token replacement into the report UI.
- Limited hero actions at the shared rendering boundary and omitted media entirely when no eligible DTO is present.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Removed the branch placeholder from its route owner**
- **Found during:** Task 3 (Extend shared server primitives for eligible media)
- **Issue:** The task required the branch route to stop consuming simulated imagery, but the declared task file list omitted the route file.
- **Fix:** Removed the `PlaceholderVisual` import/use from the Hino Cebu route and replaced it with a truthful text-led next-step panel.
- **Files modified:** `src/app/(public)/hino-cebu/page.tsx`
- **Verification:** The anchored P03-T03 contract passes, the route no longer references `PlaceholderVisual`, and strict type checking passes.
- **Committed in:** `ca82951`

**2. [Rule 1 - Bug] Restored approval-report responsive boundary behavior**
- **Found during:** Post-wave integration verification
- **Issue:** Moving public breakpoints to the UI-SPEC's exact 1059px/719px ceilings removed the existing approval report's required layouts at exactly 1060px and 720px.
- **Fix:** Added approval-report-scoped exact-boundary media rules while leaving the public UI-SPEC breakpoints unchanged.
- **Files modified:** `src/app/globals.css`
- **Verification:** The exact existing approval-report responsive test, all three anchored P03 contracts, typecheck, lint, and production build pass.
- **Committed in:** `d385fe1`

---

**Total deviations:** 2 auto-fixed (1 missing critical, 1 bug).
**Impact on plan:** The adjustments enforce media truth and preserve the existing approval-report responsive contract without introducing new assets, client state, or architecture.

## Issues Encountered

- The globally installed `gsd-sdk` executable exposes a different CLI surface; repository-compatible tracking uses `C:/Users/johnc/.codex/get-shit-done/bin/gsd-tools.cjs` directly through Node.

## User Setup Required

None - no external service configuration required.

## Known Stubs

- `src/components/ui/Shared.tsx:23` retains `PlaceholderVisual` because the governed contact and campaign surfaces still consume it. The Hino Cebu branch route no longer does; later route-specific plans own replacement or removal for the remaining surfaces.

## Validation Results

- `node --test --test-name-pattern="^P03-T01:" tests/visual-polish.test.mjs` - PASS.
- `node --test --test-name-pattern="^P03-T02:" tests/visual-polish.test.mjs` - PASS.
- `node --test --test-name-pattern="^P03-T03:" tests/visual-polish.test.mjs` - PASS.
- `node --test --test-name-pattern="^approval report is read-only and responsive without horizontal scrolling$" tests/report-ui.test.mjs` - PASS.
- `npm run typecheck` - PASS.
- `npm run lint` - PASS.
- `npm run build` - PASS.
- `git diff --check` - PASS.
- The Wave 0 RED meta-runner and full Phase 09 suite were not run, as required.

## Next Phase Readiness

- Plan 04 can consume the exact header height, responsive gutters, motion, focus, and shell surface tokens.
- Plans 05 and 06 can supply eligible media DTOs to the shared server hero and reuse stable card geometry without creating parallel components.
- Browser-level responsive and accessibility evidence remains scheduled for the phase closeout plan.

## Self-Check: PASSED

- All four declared modified files exist.
- Task commits `62b060e`, `f1c65b9`, `ca82951`, and regression fix `d385fe1` exist in repository history.
- The existing approval-report responsive contract, all three plan-owned anchored contracts, typecheck, lint, and production build are green.

---
*Phase: 01.1-public-website-visual-polish-and-engagement*
*Completed: 2026-08-22*
