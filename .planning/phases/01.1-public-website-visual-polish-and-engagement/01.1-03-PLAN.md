---
phase: 01.1-public-website-visual-polish-and-engagement
plan: "03"
type: execute
wave: 2
depends_on: [01.1-01]
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/components/ui/Shared.tsx
autonomous: true
requirements: [GOV-09, GOV-10]
must_haves:
  truths:
    - "The public site uses the approved premium-industrial Barlow 400/700 hierarchy and exact lean token system (D-01, D-03)."
    - "Cards, panels, whitespace, charcoal surfaces, and controlled Hino-red accents follow the crisp restrained contract (D-04, D-05)."
    - "Focus remains visible on white, charcoal, and Hino-red surfaces, and all motion disappears under reduced motion (D-08, D-09)."
  artifacts:
    - path: src/app/layout.tsx
      provides: "Single root Barlow font instance"
    - path: src/app/globals.css
      provides: "Approved tokens, responsive primitives, focus, and motion rules"
    - path: src/components/ui/Shared.tsx
      provides: "Optional-media PageHero and normalized CTA primitives"
  key_links:
    - from: src/app/layout.tsx
      to: src/app/globals.css
      via: "font class/variable consumed by public typography"
      pattern: "Barlow"
    - from: src/components/ui/Shared.tsx
      to: src/app/globals.css
      via: "shared hero/card/CTA class contracts"
      pattern: "page-hero|cta-band"
---

<objective>
Install the approved typography, tokens, responsive primitives, and shared hero contract without adding a UI system or client state.

Purpose: Establish the reusable visual foundation before shell and route slices consume it.
Output: Root font configuration, scoped public design tokens, and server-compatible shared primitives.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-PATTERNS.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the lean Barlow root typography contract</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <read_first>
    src/app/layout.tsx
    src/app/globals.css
    tests/governance.test.mjs
    tests/visual-polish.test.mjs
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
  </read_first>
  <action>Instantiate `Barlow` once in the minimal root layout through `next/font/google` with Latin subset, normal style, `display: "swap"`, weights exactly `400` and `700`, and Arial/sans-serif fallback; apply its class or CSS variable without moving the public shell into the root layout. In `globals.css`, restrict public typography to 16px/400/1.5 body, 14px/700/1.4 labels, 24px/700/1.2 headings, and 48px/700/1.1 display. Preserve the approval-report scoped typography and existing root/public governance split. Do not load italics, synthesize extra weights, add a font package, or introduce runtime Google requests.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P03-T01:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - One Barlow instance declares only weights `400` and `700`, Latin, normal style, and swap display.
    - Public CSS exposes exactly the four approved roles; no new intermediate public font sizes or weights are introduced.
    - Root layout remains only the HTML/body/font boundary and governance layout tests stay green.
  </acceptance_criteria>
  <done>Typography is self-hosted by Next.js, lean, legible, and architecture-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Replace public visual tokens and accessibility primitives</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-RESEARCH.md
    tests/visual-polish.test.mjs
  </read_first>
  <action>Define the exact 4/8/16/24/32/48/64 spacing scale; 1180px container; 16/24/32px breakpoint gutters; 24px grids/card padding; 64ch copy; white, `#F3F4F4`, `#17191B`, `#292C2F`, `#62676B`, `#D9DCDE`, `#D71920`, `#A50F15`, and `#1378D0` color tokens; 4px buttons, 8px cards, 12px elevated panels; and the two approved shadows. Normalize allowed transitions to 200ms ease-out and maximum 2px control/card or 8px menu translation. Preserve skip navigation and the comprehensive reduced-motion rule. Keep the blue 3px focus outline but add a contrasting separator halo/ring for controls on Hino-red surfaces so the focus indicator is not adjacent blue-on-red. Remove gradients/glow/heavy shared shadows without sweeping into scoped approval-report styles.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P03-T02:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Token values match UI-SPEC exactly and public sections use white/light-neutral with charcoal only in allowed emphasis surfaces.
    - Focus is a visible 3px blue indicator with a separating light/dark ring on red surfaces.
    - Reduced motion disables animation/transitions and smooth scrolling; normal transitions use only approved properties and distances.
    - Approval-report scoped styles are not unintentionally changed.
  </acceptance_criteria>
  <done>The public visual system is measurable, restrained, responsive, and focus-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Extend shared server primitives for eligible media</name>
  <files>src/components/ui/Shared.tsx, src/app/globals.css</files>
  <read_first>
    src/components/ui/Shared.tsx
    src/app/globals.css
    src/components/marketing/InquiryPage.tsx
    src/app/(public)/hino-cebu/page.tsx
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-PATTERNS.md
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
  </read_first>
  <action>Extend `PageHero` in place with an optional narrow eligible-media DTO and `dark | light` variant while keeping it a named Server Component, exactly one `h1`, concise description, and no more than two action children. Use `next/image` only when the DTO exists, with stable aspect-ratio container, contextual alt, and responsive `sizes`; media absence must render a complete text-led hero. Normalize `CtaBand` default label to exact `Get a Quote`. Add shared 3:2 media/body/action and responsive grid styles; only a genuinely linked whole card may lift 2px or show a pointer. Retain `PlaceholderVisual` only if another governed surface still needs it, but ensure the branch route will no longer consume it. Do not create a parallel hero/card system or a client component.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P03-T03:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `PageHero` accepts optional eligible media and dark/light variants without requiring media or adding client state.
    - Rendered media has stable geometry, responsive `sizes`, useful alt text, and no preload default.
    - Shared three/four-column grids collapse at 1060px and 720px as specified.
    - Non-interactive cards have no pointer cursor, lift, or faux focus behavior.
  </acceptance_criteria>
  <done>Shared primitives can render truthful media-rich or text-only compositions with one consistent contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eligible media DTO to shared renderer | Shared primitives must not invent or fetch public media. |
| font/image configuration to browser | Assets must remain optimized, same-origin, and layout-stable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-06 | Information Disclosure | `PageHero` | mitigate | Accept only a narrow public media DTO; never import governance records or expose withholding reasons. |
| T-01.1-07 | Denial of Service | font/media payload | mitigate | Barlow 400/700 only, no new package, no default image preload, stable responsive sizes. |
| T-01.1-08 | Elevation of Privilege | faux interactive cards | mitigate | Preserve native link/button semantics and apply interactive styling only to actual links. |
| T-01.1-SC | Tampering | package surface | accept | No install task exists; Next font/image and repository CSS are already audited dependencies. |
</threat_model>

<verification>
Run only the exact `^P03-T01:`, `^P03-T02:`, and `^P03-T03:` commands attached to their owning tasks; defer the full suite to Plan 07.
</verification>

<success_criteria>
- The exact approved visual system exists in reusable public tokens.
- The root remains server-first and adds zero runtime dependency/client island.
- Shared hero/media behavior fails closed and stays responsive.
</success_criteria>

<output>
Create `.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-03-SUMMARY.md` when done.
</output>
