---
phase: 01.1-public-website-visual-polish-and-engagement
plan: "02"
type: execute
wave: 2
depends_on: [01.1-01]
files_modified:
  - src/content/site.ts
  - src/lib/governance/public-shell.ts
autonomous: true
requirements: [GOV-09, GOV-10]
must_haves:
  truths:
    - "Find Your Truck and Hino Cebu appear only through canonical fail-closed eligibility derived from approved truck/branch data, never from route-file existence (D-07)."
    - "Facebook, YouTube, and Instagram have explicit unverified, verified, and withheld states; unavailable states expose no href (D-15, D-16)."
    - "Verified social destinations require HTTPS, an allow-listed platform host, safe public DTO fields, and no approval diagnostics (D-17)."
  artifacts:
    - path: src/content/site.ts
      provides: "Typed social records and fail-closed public profile selector"
      exports: [getEligibleSocialProfiles]
    - path: src/lib/governance/public-shell.ts
      provides: "Canonical ordered public-shell navigation and action projection"
      exports: [getPublicShellNavigation]
  key_links:
    - from: src/lib/governance/public-shell.ts
      to: src/lib/governance/eligibility.ts
      via: "eligible route projection"
      pattern: "getEligibleRoutes"
    - from: src/lib/governance/public-shell.ts
      to: src/content/site.ts
      via: "eligible branch/contact/social projection"
      pattern: "getEligibleBranch|getEligibleContactActions|getEligibleSocialProfiles"
---

<objective>
Define the server-only governance contracts that make the requested navigation and social states safe to render.

Purpose: Resolve the two route-eligibility gaps and future social activation without hardcoding withheld public destinations.
Output: Narrow typed selectors consumed later by the public layout and shell components.
</objective>

<execution_context>
@C:/Users/johnc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/johnc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-CONTEXT.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-RESEARCH.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-PATTERNS.md
@.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model and validate governed social-profile states</name>
  <files>src/content/site.ts</files>
  <read_first>
    src/content/site.ts
    src/lib/governance/approval.ts
    src/content/governance/claims.ts
    tests/governance.test.mjs
    tests/visual-polish.test.mjs
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Unverified Facebook, YouTube, and Instagram records project platform, label, and `unverified` state with no href.
    - Withheld records expose no URL, evidence, pending wording, or diagnostic reason.
    - Verified records project an href only when approval is current, scheme is HTTPS, and hostname matches that platform's explicit allow-list; malformed or mismatched values fail closed.
  </behavior>
  <action>In `src/content/site.ts`, add a closed `SocialPlatform` domain and discriminated public DTO for `unverified | verified | withheld`; only the verified variant may define `href`. Keep the private record separate from the DTO and compatible with the existing approval-envelope/evaluation pattern. Initialize Facebook, YouTube, and Instagram as unverified because no approved Hino Cebu URLs exist; do not reuse `siteConfig.socials` national URLs. Implement `getEligibleSocialProfiles(now, records)` with `URL` parsing, `https:` enforcement, per-platform host allow-lists, current approval checks for verified values, and stripped public output. Preserve branch and contact selectors exactly.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P02-T01:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript prevents `href` on unverified and withheld DTO variants.
    - Default output contains three non-clickable unverified profiles and none of the national `siteConfig.socials` URLs.
    - `http:`, malformed, cross-platform, expired, or unapproved verified records return no public href.
    - Public DTOs contain no approval envelope, evidence reference, reason, or pending value.
  </acceptance_criteria>
  <done>The footer can receive honest social states now and safe verified links later without a client-side trust decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve canonical task-first shell destinations</name>
  <files>src/lib/governance/public-shell.ts</files>
  <read_first>
    src/app/(public)/layout.tsx
    src/lib/governance/eligibility.ts
    src/content/site.ts
    src/content/trucks.ts
    src/content/governance/claims.ts
    tests/governance.test.mjs
    tests/visual-polish.test.mjs
    .planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-RESEARCH.md
  </read_first>
  <behavior>
    - Eligible output order is Trucks, Find Your Truck, Parts, Service, Hino Cebu, Get a Quote after filtering.
    - Find Your Truck exists only when the parent Trucks route is eligible and at least one eligible truck can be recommended.
    - Hino Cebu exists only when independently eligible branch identity and an eligible public branch action exist.
    - Withheld parents or absent prerequisites omit the derived destination without a gap marker.
  </behavior>
  <action>Create the server-only `src/lib/governance/public-shell.ts` contract. Export a narrow navigation DTO and `getPublicShellNavigation(now)` that consumes existing final eligible route, truck, branch, and contact-action selectors. Map governed route IDs for Trucks, Parts, Service, and Quote, and document the two derived eligibility rules for Find Your Truck and Hino Cebu in identifiers/types rather than component literals. Return only stable navigation ID, exact approved label, href, and kind (`link | primary`); never return claims, records, reasons, or approval data. Keep Quote last and omit any destination whose prerequisite is absent. Do not add governance imports to a client component.</action>
  <verify>
    <automated>node --test --test-name-pattern="^P02-T02:" tests/visual-polish.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `getPublicShellNavigation` returns the exact D-07 order after eligibility filtering.
    - Withheld `/trucks` removes both Trucks and Find Your Truck; missing eligible branch prerequisites remove Hino Cebu.
    - The module contains no fallback that treats route-file existence as eligibility.
    - The returned DTO contains no raw governance record, pending value, evidence, or diagnostic field.
  </acceptance_criteria>
  <done>The public layout has one canonical, fail-closed source for all requested task-first destinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private governance records to public shell DTOs | Pending URLs, evidence, and withheld routes must not cross the server projection. |
| approved external URL to browser link | Scheme and hostname are untrusted until server validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-03 | Spoofing | social profile selector | mitigate | Require HTTPS, current approval, and exact per-platform hostname allow-lists. |
| T-01.1-04 | Information Disclosure | public shell DTOs | mitigate | Project stable public fields only and fail closed without reasons or evidence. |
| T-01.1-05 | Tampering | derived navigation | mitigate | Derive Find Your Truck and Hino Cebu from canonical eligible parent data; never infer eligibility from files. |
| T-01.1-SC | Tampering | dependency surface | accept | No package install occurs; native URL and existing governance primitives are sufficient. |
</threat_model>

<verification>
Run only the exact `^P02-T01:` and `^P02-T02:` commands attached to their owning tasks; defer the full suite to Plan 09.
</verification>

<success_criteria>
- Social URLs cannot become public without approval plus scheme/host validation.
- Navigation is deterministic, ordered, and fail-closed for every requested destination.
- No private governance data reaches a renderable DTO.
</success_criteria>

<output>
Create `.planning/phases/01.1-public-website-visual-polish-and-engagement/01.1-02-SUMMARY.md` when done.
</output>
