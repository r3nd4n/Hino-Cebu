---
phase: 1
plan: 3
type: gap_closure
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/lib/env.ts
  - tests/env.test.mjs
  - .planning/phases/01-foundation-content-contracts-visual-system/01-VERIFICATION.md
autonomous: true
requirements: [FND-04]
gap_closure: true
---

<objective>
Close the Phase 1 verification gap by adding executable behavior tests for the server-only environment loader, including optional provider configuration and readiness states.
</objective>

<tasks>
<task type="test">
<files>tests/env.test.mjs, src/lib/env.ts</files>
<action>Add isolated executable tests that load the environment utility with controlled environment values and assert: public site URL normalization/validation; integrations remain disabled/ready when their optional credentials are absent/present; server-only module boundaries are preserved; and no secret value appears in client-facing exports. Refactor env.ts only as needed to make behavior testable without weakening validation.</action>
<verify>npm test -- tests/env.test.mjs; npm run lint; npm run build</verify>
<acceptance_criteria>Tests exercise actual environment-loader behavior rather than source-text inspection and cover missing/complete optional credential sets.</acceptance_criteria>
</task>
<task type="verification">
<files>.planning/phases/01-foundation-content-contracts-visual-system/01-VERIFICATION.md</files>
<action>Re-run the Phase 1 verification evidence for FND-04 and update the report only if the executable tests, lint, and production build pass.</action>
<verify>npm test; npm run lint; npm run build</verify>
<acceptance_criteria>The FND-04 gap is resolved with reproducible command output and the verification report no longer lists it as missing.</acceptance_criteria>
</task>
</tasks>

<verification>
Run the targeted environment tests, full test suite, lint, and production build. Confirm tests inspect runtime behavior and do not contain credentials.
</verification>

<success_criteria>
- The environment utility has executable positive and negative behavior coverage.
- FND-04 is fully evidenced by tests and build-quality checks.
- The Phase 1 verification report can be re-evaluated as passed.
</success_criteria>
