---
phase: 1
plan: 1
type: implementation
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/content/site.ts
  - src/content/trucks.ts
  - src/content/services.ts
  - src/content/navigation.ts
  - src/lib/env.ts
  - .env.example
  - .gitignore
  - README.md
autonomous: true
requirements: [FND-02, FND-03, FND-04]
---

<objective>
Create a clean, deployable Next.js TypeScript baseline with authoritative content contracts, safe environment boundaries, and the Phase 1 design tokens.
</objective>

<tasks>
<task type="implementation">
<files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
<action>Scaffold the App Router application and global layout. Configure strict TypeScript, lint/build scripts, metadata baseline, and CSS custom properties that implement the approved spacing, color, typography, button, card, focus, and responsive-container tokens. Do not build Promotions or a generic placeholder visual system.</action>
<verify>npm run lint; npm run build</verify>
<acceptance_criteria>A clean production build succeeds and the global token layer exposes the UI-SPEC palette, type, spacing, focus, and component primitives.</acceptance_criteria>
</task>
<task type="implementation">
<files>src/content/site.ts, src/content/trucks.ts, src/content/services.ts, src/content/navigation.ts</files>
<action>Create typed authoritative content modules for Hino Cebu identity/contact/hours, approved navigation, claims, vehicle ranges, services, and business uses. Use supplied approved values; preserve only genuinely absent facts as configurable unresolved values. Omit all promotion data and prevent components from owning duplicate facts.</action>
<verify>npm run lint</verify>
<acceptance_criteria>Typed modules contain the canonical public business data and no navigation/content export enables Promotions.</acceptance_criteria>
</task>
<task type="implementation">
<files>src/lib/env.ts, .env.example, .gitignore, README.md</files>
<action>Define server-only runtime environment handling and a complete no-secret `.env.example` for future leads, analytics, and site URL integrations. Document local bootstrap and the explicit rule that only `NEXT_PUBLIC_*` values may enter client code.</action>
<verify>npm run build</verify>
<acceptance_criteria>Required integration variable names are documented, no actual secret is committed, and server environment utilities cannot be imported by client components.</acceptance_criteria>
</task>
</tasks>

<verification>
Run lint and production build. Inspect the content exports and env example to confirm no Promotions surface or secret value exists.
</verification>

<success_criteria>
- A deployable, strict TypeScript Next.js baseline exists.
- Approved site data has one typed authority.
- UI tokens match the Phase 1 contract.
- Environment and documentation safeguards are present.
</success_criteria>
