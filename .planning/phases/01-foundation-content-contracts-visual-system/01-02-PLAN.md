---
phase: 1
plan: 2
type: implementation
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/MobileActionBar.tsx
  - src/components/layout/MobileMenu.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/Container.tsx
  - src/components/ui/Icon.tsx
  - tests/foundation.test.mjs
  - README.md
autonomous: true
requirements: [FND-01, FND-04]
---

<objective>
Implement the reusable responsive Hino Cebu public shell and UI primitives that later phases can compose without redesigning navigation, actions, or basic components.
</objective>

<tasks>
<task type="implementation">
<files>src/components/ui/Button.tsx, src/components/ui/Container.tsx, src/components/ui/Icon.tsx</files>
<action>Build accessible primitives using the Phase 1 token contract: primary/secondary buttons, responsive container, and a consistent Lucide icon adapter. Enforce the non-pill shape, visible focus treatment, 48–54px action height, and semantic button/link behavior.</action>
<verify>npm run lint; npm run build</verify>
<acceptance_criteria>Primitives are reusable, keyboard accessible, and visually conform to the UI-SPEC without importing a third-party registry.</acceptance_criteria>
</task>
<task type="implementation">
<files>src/components/layout/Header.tsx, src/components/layout/Footer.tsx, src/components/layout/MobileActionBar.tsx, src/components/layout/MobileMenu.tsx, src/app/layout.tsx</files>
<action>Implement the reference-aligned public shell using authoritative content data: desktop white header with specified navigation and red phone action; dense dark footer; near-full-screen accessible mobile menu; and sticky mobile Call/Request a Quote actions. Exclude Promotions from every rendered surface and preserve focus/scroll behavior for the menu.</action>
<verify>npm run lint; npm run build</verify>
<acceptance_criteria>Desktop and mobile shell render from shared content data, use the correct Cebu phone link, expose clear quote/call paths, and contain no Promotions item/link/route reference.</acceptance_criteria>
</task>
<task type="test">
<files>tests/foundation.test.mjs, README.md</files>
<action>Add focused tests that assert approved navigation excludes Promotions, the site config supplies the required public contact data, and public shell exports required actions. Document how to run quality checks and manually inspect 390px, 768px, 1024px, and 1440px layouts.</action>
<verify>npm test; npm run lint; npm run build</verify>
<acceptance_criteria>Foundation regression tests pass and the README provides repeatable developer verification steps.</acceptance_criteria>
</task>
</tasks>

<verification>
Run tests, lint, and build; inspect desktop/mobile header behavior and ensure the mobile action bar never overlaps primary content.
</verification>

<success_criteria>
- Visitors receive an accessible, responsive public shell with correct local actions.
- Future pages can reuse consistent primitives rather than restyling controls.
- Promotions remain absent by design and by test coverage.
</success_criteria>
