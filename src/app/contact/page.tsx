import { Breadcrumbs, Container, JsonLd, PageHero, PlaceholderVisual } from "@/components/ui/Shared";
import { TrackedLink } from "@/components/marketing/TrackedLink";
import { directionsHref, siteConfig } from "@/content/site";
import { createMetadata } from "@/lib/seo";
import { absoluteUrl } from "@/lib/site-url";
export const metadata = createMetadata({ title: "Contact Hino Cebu | Location & Phone", description: "Call Hino Cebu or view the branch address at 377 P. Almendras Extension, Cebu City.", path: "/contact" });
export default function ContactPage() { const schema = { "@context": "https://schema.org", "@type": "AutoDealer", name: siteConfig.name, url: absoluteUrl("/contact"), telephone: siteConfig.phoneDisplay, address: { "@type": "PostalAddress", streetAddress: "377 P. Almendras Extension", addressLocality: "Cebu City", addressRegion: "Central Visayas", addressCountry: "PH" } }; return <><Container><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Contact" }]} /></Container><PageHero eyebrow="Contact and location" title="Connect with Hino Cebu" description="Call the branch, request support through the right form, or use the verified address below." /><section className="section" id="location"><Container className="split"><div className="location-card"><h2>Verified branch details</h2><address><strong>{siteConfig.name}</strong><br />{siteConfig.address}<br /><a href={siteConfig.phoneHref}>{siteConfig.phoneDisplay}</a></address><p>Address and phone were verified from the stakeholder-provided Hino Cebu business listing image. Operating hours remain pending. Directions currently use an address-based Google Maps search until the official Business Profile URL is supplied.</p><div className="hero-actions"><TrackedLink className="button" href={siteConfig.phoneHref} event="phone_click" eventProperties={{ location: "contact" }}>Call Hino Cebu</TrackedLink><TrackedLink className="button button-outline" href={directionsHref} event="directions_click" eventProperties={{ location: "contact" }}>Directions</TrackedLink></div></div><PlaceholderVisual label="Branch exterior and map" /></Container></section><JsonLd data={schema} /></>; }
