import Link from "next/link";
import { Breadcrumbs, Container, PageHero } from "@/components/ui/Shared";
import { activePromotions } from "@/content/promotions";
import { createMetadata } from "@/lib/seo";
export const metadata = createMetadata({ title: "Hino Cebu Promotions | Current Verified Offers", description: "View currently published and verified Hino Cebu promotions, or contact the team for current information.", path: "/promotions" });
export default function PromotionsPage() { const promotions = activePromotions(); return <><Container><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Promotions" }]} /></Container><PageHero eyebrow="Verified offers" title="Hino Cebu promotions" description="Only active, approved promotion data appears here. Expired or unverified offers are not presented as current." /><section className="section"><Container>{promotions.length ? <div className="grid grid-3">{promotions.map((promotion) => <article className="card card-body" key={promotion.slug}><h2>{promotion.title}</h2><p>{promotion.summary}</p><Link className="button" href={promotion.ctaHref}>{promotion.ctaLabel}</Link></article>)}</div> : <div className="empty-state"><h2>No published promotion right now</h2><p>We do not invent discounts, pricing, financing rates, or availability. Contact Hino Cebu for current, confirmed information.</p><Link className="button" href="/contact">Contact Hino Cebu</Link></div>}</Container></section></>; }
