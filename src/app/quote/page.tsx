import { InquiryPage } from "@/components/marketing/InquiryPage";
import { createMetadata } from "@/lib/seo";
export const metadata = createMetadata({ title: "Request a Hino Truck Quote Cebu | Hino Cebu", description: "Tell Hino Cebu about your business use, truck interest, timing, and financing intent to request sales follow-up.", path: "/quote" });
export default async function QuotePage({ searchParams }: { searchParams: Promise<{ model?: string }> }) { const { model } = await searchParams; const normalized = model ? `Hino ${model.replace("hino-", "")}` : ""; return <InquiryPage type="sales" eyebrow="Sales inquiry" title="Request a Hino truck quote" description="Share your truck interest and business use so Hino Cebu can follow up with the right conversation." formTitle="Tell us what your business needs" submitLabel="Request a Quote" defaults={{ modelInterest: normalized }} points={["Choose a model family or select Not sure", "Describe the work and purchase timeline", "Indicate whether financing is part of the conversation"]} note="A submitted form is not a price, inventory, technical-suitability, or financing guarantee. All details require confirmation." />; }
